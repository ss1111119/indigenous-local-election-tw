## 1. 算繪與寫入

- [x] 1.1 讓 `render_markdown()` 在既有的本地選舉與立委區塊之後，再呼叫一次 `_render_manifest_sections(PARTY_LIST_MANIFEST, names)`，`names` 對照表涵蓋 `party_list_summary`（例如「政黨票選舉概況 summary」）、`party_list_votes`（「政黨票候選人得票 votes」）、`party_list_seats`（「政黨票席次 seats」）三個鍵，實作 Requirement「Every declared column manifest is rendered into the shared oracle document」的「The party-list manifest gains a rendered section」情境。驗證：新增測試呼叫 `render_markdown()`，斷言輸出字串同時含三個政黨票區塊標題，且每個區塊列出的欄位名稱集合與 `PARTY_LIST_MANIFEST` 對應鍵的欄位名稱集合完全相同（不多不少）。
- [x] 1.2 在 `scripts/build_party_list_election.py` 的 `main()` 收尾（`commit_outputs(...)` 呼叫之後）新增呼叫 `write_oracle_document()`，實作 Requirement「Every declared column manifest is rendered into the shared oracle document」的「The legislative and local-election sections stay unaffected by the party-list addition」情境。驗證：依序執行 `python scripts/build_party_list_election.py`、`python scripts/build_legislative_election.py`、`python scripts/build_local_election.py`，用 `git diff --stat docs/schema/oracles.md` 確認三次執行後檔案內容逐位元組相同（新增的政黨票區塊不會因為其他兩支腳本重新執行而被覆寫掉，本地與立委區塊也不因為這次改動而變動）。
