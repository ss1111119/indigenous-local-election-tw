## 1. 補償檢查先改，再動欄位

- [x] 1.1 把補償檢查改為比對「由 `當選註記` 推導的值」與權威值，落實設計決策「⚠️ 補償檢查必須改為比對「來源推導值 vs 權威值」，否則會靜默失效」。推導用既有的 `ELECTED_MARKS` 常數，不新增第二份判準。此步驟**必須在對調欄位之前完成**——順序相反的話，中間會有一段檢查已失效而沒人察覺的狀態。驗證方式：在尚未對調欄位的情況下重建，斷言那 63 筆具名異常仍全數被列出（2005 T2 33 筆、2005 T3 28 筆、1994 T-COMBO 2 筆），且未出現任何新的不符。
- [x] 1.2 為該檢查新增**兩項**變異，實現 Elected Status Compensating Checks：①把比對改回兩個權威值；②把 `KNOWN_ELECTED_MARK_ANOMALIES` 清空。後者是恆真同義反覆的偵測器——白名單清空後建置必須中止，若仍通過代表那個檢查早已失去對真實異常的辨識力。實測目前沒有任何變異在測第二項。驗證方式：執行建置端變異測試腳本，斷言兩項皆被偵測到——辨識力來自真實資料裡的那 63 筆，不需要合成資料。

## 2. 對調欄位

- [x] 2.1 `當選` 改存權威值（維持 `Y`／`N` 編碼），自長表移除 `elected_authoritative`，並把 `elected_authoritative_basis` 更名為 `當選_依據`。實現 Authoritative Elected Status Derivation，落實設計決策「`當選` 改存權威值，`elected_authoritative` 移除」與「維持 `Y`／`N` 編碼，不改成 `true`／`false`」。驗證方式：重建後以指令讀出，斷言欄位數 29→28、`當選` 逐列等於重建前的 `elected_authoritative`（`true`→`Y`）、`當選_依據` 逐列等於重建前的 `elected_authoritative_basis`、且長表不再含 `elected_authoritative` 欄。
- [x] 2.2 確認來源原樣未受影響，落實設計決策「不新增 `當選_原始`」——來源的當選與否已完整存在於 `當選註記`（原樣）與 `當選註記語意`（解碼），不再另立第三份表述。驗證方式：斷言 `當選註記` 與 `當選註記語意` 兩欄逐列與重建前相同，且 summary 與 votes 兩張長表的 SHA-256 不變。
- [x] 2.3 更新 oracle 清單：`當選` 改為 `provenance=project`、note 寫明它現在是權威值且來源原樣在 `當選註記`；移除 `elected_authoritative` 條目；新增 `當選_依據`。驗證方式：執行既有的清單完整性檢查（`check_manifest`）確認通過，並以指令確認清單中已無 `elected_authoritative`。

## 3. 站台端

- [x] 3.1 站台產生器改讀 `當選`（值為 `Y`），移除對 `elected_authoritative` 的所有引用，並更新必要欄位宣告。落實設計決策「`當選` 改存權威值，`elected_authoritative` 移除」中的「同一事實不要有兩份來源」。驗證方式：站台 `--check` 通過且兩個 HTML **位元組不變**；欄位契約測試（宣告與實際讀取兩個方向）通過。
- [x] 3.2 更新站台測試與變異：既有的「席次取自權威值而非 `當選`」一組測試其前提已改變（兩者現在是同一欄），改為驗證「席次取自 `當選`，而 `當選` 是權威值」，並保留對 `當選註記` 的判別——把席次改成由 `當選註記` 推導必須失敗。驗證方式：`pytest` 通過；站台變異全部被偵測到且基準對照通過。

## 4. 文件

- [x] 4.1 更新資料集說明文件與專案說明文件：`當選` 現為權威值、來源原樣在 `當選註記`／`當選註記語意`、`elected_authoritative` 已移除、`當選_依據` 取代 `elected_authoritative_basis`。並在 `data/sources.json` 的具名瑕疵中記下同名欄位的語意在本變更前後不同。並在說明文件中**明確警告「總欄位數由 29 變 28，不可用位置索引取值」**——移除欄位會使其後所有欄位左移一格，依位置讀取的下游不會收到錯誤訊息，只會拿到下一欄的字串。驗證方式：以指令搜尋三份文件，確認不再出現「要席次請用 `elected_authoritative`」這類已不成立的指示，且 `當選_依據` 與該欄位數警告皆已出現。
