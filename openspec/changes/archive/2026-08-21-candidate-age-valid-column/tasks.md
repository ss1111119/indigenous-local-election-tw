## 1. 建置端新增欄位（必須先做完，站台端才能改）

- [x] 1.1 在長表建置腳本的候選人列組裝處把 `年齡` 改為**乾淨值**（`0` 在任何屆別都留空、`99` 只在具名的五個屆別留空、其餘放原值），來源原值改寫入緊接其後的 `年齡_原始`。實現 The Plainest Column Name Holds The Safest Data、A Cleaned Column Holds Values, Not A Validity Flag，以及改寫後的 Sentinel Values Are Not Presented As Measurements（替換改在長表發生，不再只在呈現端），落實設計決策「乾淨的值放在 `年齡`，來源原值退居 `年齡_原始`」與「不用布林旗標（`年齡` 放值，不放 true／false）」。驗證方式：重建後以指令讀出，斷言欄位數 29、欄序為 `年齡` 後接 `年齡_原始`、五個舊屆的 `年齡` 全為空、2009-2010 以後四屆的 `年齡` 與 `年齡_原始` 逐列相同、且 `AVG(年齡)` 得到 n=7,335／平均 50.80。

  ⚠️ 本任務原文是「新增 `年齡_有效`、`年齡` 完全不動」。外部覆核指出那只開了安全通道、沒封閉陷阱——不讀文件的人第一直覺永遠是 `AVG(年齡)`。使用者裁決改為對調，設計決策 1 已改寫並記下兩項代價。
- [x] 1.2 把兩條守住具名清單前提的斷言放進建置端，落實設計決策「判準與站台端一致，且只有一個實作」中「斷言隨判準一起移動」：具名為未記載的屆別出現第三種值（非 `0` 非 `99`）即中止並指出屆別與該值；未具名的屆別出現 `99` 即中止並指出屆別。驗證方式：以合成資料分別注入兩種情形，斷言各自中止且訊息含屆別；未注入時正常通過。
- [x] 1.3 更新 oracle 清單，實現設計決策「兩個欄位都必須進 oracle 清單」：`年齡` 改為 `provenance=project`，note 寫明判準、空字串的意義，以及「乾淨不等於正確」——來源記載的年齡本身正確與否本專案未查證；新增 `年齡_原始` 條目（`provenance=official`）並指向 `年齡`。驗證方式：執行既有的清單完整性檢查，確認通過；並以指令搜尋兩個條目，確認 note 含上述澄清。
- [x] 1.4 確認未覆寫來源值。驗證方式：逐列比對重建前後的 `年齡` 欄，斷言完全相同；並斷言 summary 與 votes 兩張長表的 SHA-256 與重建前相同。

## 2. 站台端改為讀取，移除重複實作

- [x] 2.1 站台產生器改為讀取已清乾淨的 `年齡`，刪除自己那份 `AGE_UNRECORDED_TERMS`、`AGE_ALWAYS_NO_DATA` 與 `display_age` 的推導邏輯，且**不讀** `年齡_原始`。實現 One Implementation Of A Sentinel Rule。驗證方式：欄位契約測試（宣告與實際讀取兩個方向）通過；站台 `--check` 通過且兩個 HTML 位元組不變。
- [x] 2.2 調整站台測試與變異測試以對應新結構：移除已搬走的判準測試，改為**判別測試**——餵入 `年齡_原始=99`／`年齡=45` 的衝突列，讀 `年齡` 得 45、自己重算得 None，兩者不可能同時成立；建置端的斷言各配一項變異。驗證方式：`pytest` 通過；站台變異全部被偵測到且基準對照通過。

## 3. 文件

- [x] 3.1 在資料集說明文件的欄位表改寫 `年齡` 並新增 `年齡_原始`，並在專案說明文件更新候選人長表的欄位數敘述。驗證方式：以指令搜尋兩份文件，確認 `年齡_原始` 出現且欄位數敘述為 29；並確認文件明寫「乾淨不等於正確」而非對年齡正確性背書。
