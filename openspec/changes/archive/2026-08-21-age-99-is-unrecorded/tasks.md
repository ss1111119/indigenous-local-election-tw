## 1. 辨識哨兵值並守住前提

- [x] 1.1 在站台資料產生腳本中建立 `AGE_UNRECORDED_TERMS`（恰含 1994、1998、2002、2005、2006 五個屆別字串）與 `AGE_UNRECORDED_VALUE`（`99`），並讓名錄的候選人 tuple 在這五屆把年齡位置給 `0`。實現 Sentinel Values Are Not Presented As Measurements 與設計決策「在呈現端辨識，長表保持來源原樣」。驗證方式：產生名錄常數後以指令讀出，斷言五個舊屆所有候選人 tuple 的第五個位置皆為 `0`，且 2022 屆的年齡相異值仍為 57 個。
- [x] 1.2 加入兩條會中止建置的斷言，實現 Sentinel Recognition Is Named Per Term, Not Global 與設計決策「具名到屆別，不是「99 一律當未記載」」（無條件規則會在將來真有 99 歲候選人時默默吃掉真值）：清單中的屆別其年齡相異值必須恰為 `99`（否則中止並指出屆別與多出來的值）；清單外的屆別不得出現 `99`（否則中止並指出屆別）。兩條都在讀入長表後、計算之前執行。驗證方式：以合成長表分別注入「舊屆出現非 99 的年齡」與「新屆出現 99」兩種情形，斷言各自拋出 `SiteDataError` 且訊息含該屆別；未注入時正常通過。
- [x] 1.3 確認三張長表未被更動。驗證方式：比對三張長表在本變更前後的 SHA-256，斷言完全相同。

## 2. 測試與變異

- [x] 2.1 在站台測試檔新增合成資料測試，涵蓋 1.1 與 1.2 兩項行為：舊屆的 `99` 轉為 `0`、新屆的真實年齡原樣保留、兩條斷言各自會中止。驗證方式：`pytest` 通過，且測試以合成長表而非真實資料驅動斷言的中止路徑（真實資料永遠不會觸發那兩條中止）。
- [x] 2.2 在站台變異測試腳本新增三項變異並確認全部被偵測到：把 `AGE_UNRECORDED_TERMS` 清空（舊屆的 99 會被當年齡顯示）、把判準改成無條件「99 一律當未記載」（新屆真有 99 歲時會被吃掉）、以及拿掉兩條斷言其中之一。驗證方式：執行該腳本，斷言含新增項在內的所有變異皆被偵測到，且基準對照通過。

## 3. 呈現與記錄

- [x] 3.1 以瀏覽器開啟名錄頁確認呈現正確，落實設計決策「名錄不顯示「未記載」字樣，直接省略」——那五屆整批未記載是資料集層級的事實，逐筆重複同一句話佔空間又不帶新資訊，該事實改記在具名瑕疵與說明文件（見 3.2）。驗證方式：`#1998/T3` 與 `#2005/T3` 的候選人滑鼠提示只顯示性別、不含「歲」；`#2022/T2` 的滑鼠提示仍含年齡；console 無 error 與 warn。
- [x] 3.2 把本瑕疵加入 `data/sources.json` 既有的具名瑕疵清單（與另外八項同一處，沿用 `id`／`summary`／`scope`／`detail`／`silent` 的鍵），並在資料集說明文件中記下該五屆未記載年齡。實現 Column Semantics Are Taken From The Source Format Document First：敘述須**引用**壓縮檔內的官方格式文件（`votedata/voteData/選舉資料庫格式.odt` 對 elcand.csv 年齡欄的「可能 0 或 99」），並記下與之一致的實測分布。

  ⚠️ 本任務原文寫「不得寫成『來源明定』——格式文件是否有此定義未查證」。**那是錯的**：查證做得到，而且 `docs/schema/cec-local-election.md` 早就記著這一條。欄位語意的權威是壓縮檔內的格式文件，查它是動手前的第一步。已更正。

  驗證方式：以指令搜尋新增的 `id`，確認存在且 `silent` 為 true；確認 `detail` 含格式文件的檔名與引文；並確認全文沒有「未查證」這類與事實不符的措辭。
