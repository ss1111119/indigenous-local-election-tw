## 1. 建置腳本骨架與來源路徑

- [x] 1.1 建立 `scripts/build_legislative_election.py`，落實設計決策「另立建置腳本，不擴充地方公職的建置腳本」。低階工具（`read_csv`、`admin_level`、`is_blank`）以 import 沿用 `scripts/build_local_election.py`，**不搬動也不修改該檔**。腳本此步只需能被匯入並印出可用的屆別清單。驗證方式：`python -c "import build_local_election"` 與新腳本各自匯入成功；且 `git diff --stat scripts/build_local_election.py` 為空。
- [x] 1.2 建立十八個來源檔的**具名路徑表**（九屆 × 山原／平原），實現 Requirement: Superseded Duplicate Source Directories Are Excluded By Name 與 Requirement: Nine Terms Of Indigenous Legislative Elections Are Covered。2016 的檔名為 `elcand_T3.csv` 形式須各自具名，不以萬用字元推導；任何含 `old` 路徑段的目錄一律排除。驗證方式：以指令列出解析結果，斷言恰為 18 個目錄且每個目錄含 elbase／elcand／elctks／elprof／elpaty 五個檔；另以合成輸入（把一個 `old` 路徑塞進路徑集合）斷言中止，錯誤訊息指出該路徑。
- [x] 1.3 實作鍵欄前置單引號的剝除，落實設計決策「三項具名來源瑕疵與其補償性檢查」之第一項，實現 Requirement: A Change In Key Formatting Aborts Rather Than Silently Failing To Match。**僅對具名的（屆別, 選舉種類, 檔）剝除**，且雙向檢查：宣告有引號卻讀到沒有、宣告沒有卻讀到有，皆中止。已清點 90 個來源檔，具名者為 **2008 的 `elcand`**（同屆其餘檔皆無）與 **2012 的 `elbase`／`elcand`／`elctks`／`elprof`**（`elpaty` 無），共 10 個檔（L2、L3 各 5 個）。驗證方式：斷言那 10 個具名檔解析後不含引號、其餘 80 個檔的值與來源位元組相同。⚠️ 「改成全域剝除」這項變異**在乾淨資料上不會改變任何輸出**（那 10 個檔剝不剝除，對其餘 80 個檔的輸出毫無影響），必須另建一份合成來源：把引號加到一個未具名的檔上，斷言全域剝除版本靜默通過而具名版本中止。另一項變異（把 2008 的 `elcand` 從宣告移除）可直接以真實資料觸發——雙向檢查會因「未宣告卻讀到引號」而中止。兩項皆須被偵測到。

## 2. 解析與長表輸出

- [x] 2.1 逐（屆別, 選舉種類, 檔）宣告**選舉區欄允許值**，落實設計決策「選舉區欄的允許值逐屆逐檔具名宣告」，實現 Requirement: The Nationwide Constituency Is Stated, Not Implied，並輸出 `選舉區_語意` 欄記錄該屆此欄是否帶選區意義（本批全部為「無選區意義（全國單一選區）」）。已清點 90 個檔：取值由**檔**決定而非由屆別決定——`elbase` 九屆恆為 `00`、`elcand` 九屆恆為 `01`、`elctks` 與 `elprof` 在 1995–2008 為 `00`、2012 起為 `00` 與 `01`；L2 與 L3 形態一致。驗證方式：斷言 90 個檔的實測取值集合逐檔等於宣告集合、且 `選舉區_語意` 在十八個（屆別, 選舉種類）皆為該固定字串；另加一項變異（把某屆的允許值宣告錯），須被偵測到。
- [x] 2.2 逐屆宣告最細層級，實現設計決策「層級深度逐屆宣告，不以「有多少列」推斷」與 Requirement: A Term's Breakdown Is Declared, And Narrows Only Deliberately。已實測：1995–2004 最細到鄉鎮市區、2008 起到投開票所。驗證方式：斷言九屆解析後實際達到的最細層級等於宣告值；另加一項變異（把層級檢查拿掉），須被偵測到——該變異在乾淨資料上不改變輸出，所以測試必須以合成的「細層級整批遺失」輸入來觸發它。
- [x] 2.3 組裝候選人長表的列，欄位依 design 的 Implementation Contract 所列，實現設計決策「個資欄位一律不輸出」與 Requirement: Personal Data Fields Are Never Published。`年齡` 存乾淨值、`年齡_原始` 存來源原值——⚠️ 普查確認哨兵 `99` 涵蓋 **1995／1998／2001／2004 四屆且是整批**（那四屆 72 人全部是 `99`，2008 起完全沒有），**不可沿用 `build_local_election.AGE_UNRECORDED_TERMS`**（那是地方公職的屆別，只有 1998 重疊）；`當選註記` 帶尾隨空白（九屆共 103 列），解讀前必須 `strip()`；**出生日期、出生地、學歷三欄不得出現在輸出的任何位置**；`當選` 與 `當選_依據` 此步先留空佔位以固定欄序。驗證方式：以合成來源實跑列組裝函式（不是讀成品），斷言輸出欄名集合恰等於契約所列、且不含那三個個資欄；另加一項變異（把出生日期加進輸出），須被偵測到。
- [x] 2.4 由 elctks 跨檔推導當選權威值填入 `當選`（`Y`／`N`）與 `當選_依據`——⚠️ 普查確認 `elcand` 與 `elctks` 的**整組鍵相符數在十八個檔全部為 0**（`elcand` 選舉區欄恆為 `01`、`elctks` 為 `00`），忽略選舉區欄後 100% 對上，因此跨檔推導**必須全面忽略選舉區欄**，這在立委是常態而非具名例外——並加上比對「由 `當選註記` 現算的值 vs `當選`」的補償性檢查，落實設計決策「`當選` 欄的語意與地方公職一致」，實現 Requirement: Elected Status Follows The Project's Established Column Convention。**比對的兩側必須取自不同欄位**——兩側同源會恆不成立、一筆都收不到而不報錯。⚠️ **驗證不可只斷言「不符集合為空」**——本批來源沒有損壞，那條斷言在補償性檢查被整段刪掉時仍會通過，是一條無法失敗的驗證。必須另建一份合成來源：把某一位候選人的 `當選註記` 改成與 elctks 推導結果相反，斷言建置中止且錯誤訊息指名到該候選人。驗證方式：前述合成輸入的中止行為；十八個真實檔的不符集合為空（作為輔助斷言，不作為主要驗證）；另加兩項變異（比對改成兩側同讀 `當選`、把推導改成直接抄 `當選註記`），皆須被前述合成輸入偵測到。
- [x] 2.5 產出 summary 與 votes 兩張長表並確認既有輸出未受影響，實現 Requirement: Legislative Data Is Published Separately From Local Office Data。欄位比照既有地方公職長表的對應表，去除 `is_main_sequence`（主序列過濾只對地方公職成立），保留 `縣市_正規化` 與 `鄉鎮市區_正規化`。驗證方式：斷言三張新長表的列數與各屆別選舉種類的分布可重現、summary 的全國合計列在九屆各一（山原、平原各自成立）；並斷言 `data/processed/cec-local-election-*` 三張長表與 `validation-report.json` 的 SHA-256 與本變更前相同。
- [x] 2.6 逐屆釘死應選名額並與 `elprof` 的當選人數欄交叉比對，實現 Requirement: A Term's Seat Total Is Fixed By History, Not By Whatever The Source Currently Says。釘死值為 1995 各 3、1998／2001／2004 各 4、2008 起各 3。⚠️ 讀取 `elprof` 的候選人數與當選人數**必須沿用既有的 `detect_layout()` 逐檔偵測**，不可依官方格式文件的欄位順序假設——普查確認 **2024 兩個檔是「男女合計」版面、其餘十六個檔是「合計在前」**。另 2004 的 `elprof` 有效票／無效票／投票數／選舉人數四欄各有 368 列帶尾隨空白，轉數值前須 `strip()`。驗證方式：斷言十八個檔的 `elcand` 當選數、`elprof` 當選人數、釘死值三者相符；另加一項變異（把某屆的釘死值改掉），須被偵測到。

- [x] 2.7 產出 `縣市_正規化` 與 `鄉鎮市區_正規化` 兩欄並建立立委屆別的縣市代碼對照表，落實設計決策「保留並擴充地理正規化欄位」，實現 Requirement: Geographic Keys Are Not Published As Falsely Joinable。已普查的重編事實：**縣市代碼有三個系統**——1995–2008 為省市 `03`／`04`（23 個縣市）、2012 為 `06`／`07`（17 個，五都升格）、2016–2024 為 `09`／`10`（16 個，桃園升格）。⚠️ **2012 那一屆的縣市碼整組重編且與其他兩期都不同**：宜蘭縣在 1995 與 2020 都是 `002`、在 2012 是 `001`，而 `001` 在 1995 是**臺北縣**——跨屆 join 會把宜蘭的資料接到臺北縣上，成功執行且無錯誤訊息。1995–2008 與 2016–2024 的編號恰好相同，使得「抽兩屆比對就以為安全」特別容易發生。秀林鄉的鄉鎮市區碼 1995 為 `011`、2004 為 `003`、2020 為 `110`；烏來 1995 為 `029`、2004 為 `024`，2020 已改制為新北市烏來區 `65/000/290`。**無法對應者 `鄉鎮市區_正規化` 一律留空，不得放原始碼。** ⚠️ 另已實測：`elbase` 的選舉區欄恆為 `00`，而 `elprof`／`elctks` 從 2012 起為 `01`，以整組五碼鍵查行政區名稱時 2012–2024 每屆有 8,000 個以上單位查不到（2012 實測 8,175）——**行政區名稱的查詢必須忽略選舉區欄**。 對照表新增於 **`data/reference/cec-legislative-county-crosswalk.csv`**——刻意不放 `data/processed/`，因為它是**輸入**不是產物；`HANDOFF.md`「下一步」第 5 項記著地方公職的對照表放在產物目錄，而「清空輸出目錄再重跑」會把它一起刪掉然後建置失敗。驗證方式：斷言以 `縣市_正規化` 跨屆 join 時，秀林鄉在 1995／2004／2020 三屆都對到同一個識別；斷言 `鄉鎮市區_正規化` 在未建立對照的屆別為空字串而非原始碼；另加兩項變異（正規化欄直接放原始碼、對照表少一列），皆須被偵測到——後者的辨識力來自對照表不得有從未被使用的列這項既有檢查。

- [x] 2.8 為第二輪普查新增的四項具名瑕疵各建立清單與補償性檢查，實現 Requirement: Aggregate Rows That Disagree With Their Own Detail Are Named, And The Detail Wins。四項為：①1995 四個號次的檔別合計與逐縣市加總差 ±1（L2 號次 1 與 3、L3 號次 3 與 4）；②2001 平地立委臺北市有效票差 22；③2004 平地立委臺中縣 +58／彰化縣 −58 的兩縣互換；④1998 山地立委三列投票率寫 `0.00` 而重算為 66.67% 與 25.00%。驗證方式：斷言四份清單的不符集合恰等於具名集合（多一筆少一筆都中止）；另加四項變異（各清空一份清單），皆須被真實資料觸發而中止——這四項的辨識力來自真實資料本身，不需要合成輸入。

- [x] 2.9 把 2016 的最細層級降級到鄉鎮市區並排除村里與投開票所列，落實設計決策「三項具名來源瑕疵與其補償性檢查」第二項的修訂，並實現 Requirement: A Term's Breakdown Is Declared, And Narrows Only Deliberately 的「刻意降級須與來源深度分開宣告」。⚠️ 設計決策「實作前的完整普查結果」列出的逐檔事實是本 task 各項釘死值的依據。已實測：2016 正式版的村里層級有 1,402 個單位在 `elctks` 無對應、50 個單位有效票不符，投開票所層級有 4,396 個無對應；鄉鎮市區以上完全相符。⚠️ **不輸出已知不完整的數字**，而不是輸出後在文件加註。驗證方式：斷言 2016 的三張長表不含任何村里或投開票所層級的列、且鄉鎮市區以上的有效票與 `elctks` 加總零不符；斷言其餘八屆的最細層級未受影響（2008／2012／2020／2024 仍到投開票所、1995–2004 仍到鄉鎮市區）；另加一項變異（把 2016 的降級宣告移除），須因村里層級的 50 個不符而中止。

## 3. Oracle 與驗證報告

- [x] 3.1 為新的三張長表在 `scripts/oracles.py` 建立欄位 oracle 清單，每個欄位標明 provenance（official／project）與結構說明，並註記 `L2`／`L3` 為本專案自訂代碼。`當選` 標為 project 並註明其為權威值、來源原樣在 `當選註記`；`選舉區_語意` 標為 project。驗證方式：執行既有的清單完整性檢查（`check_manifest`）確認新清單通過，且三張長表的實際欄名與清單逐一對應、無多無缺。
- [x] 3.2 產出 `data/processed/legislative-validation-report.json`，內容含各屆別選舉種類的候選人數、當選人數（由 `當選註記` 推導）、當選人數_權威值（由 `當選` 推導）、選舉人數、投票數、投票率，以及來源檔 SHA-256。**兩個當選人數必須取自不同欄位**。驗證方式：斷言報告涵蓋九屆十八個檔，且 2020 山原的選舉人數為 215,115、投票率 68.6%。

## 4. 測試與變異測試

- [x] 4.1 建立 `scripts/test_build_legislative_election.py`，釘死席次序列與 2020 的外部錨點值：山原選舉人 215,115、投票率 68.6%、第二名與第三名得票差 16 票；平原選舉人 199,833、投票率 62.3%。驗證方式：`pytest scripts/` 全數通過；且刻意把任一釘死值改錯時該測試必須失敗（人工確認一次並記錄結果）。
- [x] 4.2 建立 `scripts/mutate_build_legislative_election.py`，收納前述各步新增的變異項目，並比照既有兩支變異腳本的規格：副本放在 repo 根目錄下一層的 `_mut/`、檔名不得為 `test_*.py` 或 `*_test.py`、執行部分包在 `main()` 裡、並以基準對照確認未變異時通過且無測試被跳過。驗證方式：執行該腳本，斷言所有新增變異皆被偵測到、基準對照通過、且結束後 `_mut/` 已清除。

## 5. 文件

- [x] 5.1 新增 `docs/schema/cec-legislative-election.md` 並更新 `data/sources.json`、`README.md`、`HANDOFF.md`。文件須寫明：九屆的席次逐屆不同（不可寫成單一數字）、`L2`／`L3` 是本專案自訂代碼而非官方代碼、選舉區欄無選區意義、以及三項具名來源瑕疵（2012 引號、2016 的 `old/` 差異未查明、席次跨屆變動）。`data/sources.json` 須把報導者該文記為「複核用對照」而非資料來源。驗證方式：以指令搜尋四份文件，確認 `L2`／`L3` 的自訂代碼註記、逐屆席次表、以及三項瑕疵皆已出現，且未出現「立委席次固定 3 席」這類不成立的敘述。
