## 1. 山地鄉名單的查證與落地

- [x] 1.1 查證官方公告的山地鄉名單來源（原住民族委員會或行政院公告），記下公告文號、發布日期、生效期間與可查證的 URL，實現規格「The mountain indigenous township list is sourced from an official designation and scoped by effective period」，並依設計決策「山地鄉名單以官方公告為權威來源，並記錄生效期間而非單一快照」辦理。驗證：找到的來源可被重新開啟並確認含完整鄉鎮清單；若查無官方公告，則在清點報告中明寫「查無官方公告」並停止本任務群組後續步驟，不以次級來源替代。
- [x] 1.2 建立 `data/reference/mountain-township-list.csv`，欄位含縣市名稱、鄉鎮市區名稱、生效起、生效迄、出處，改制離開該類別的單位保留該列並填生效迄。驗證：檔案每一列的出處欄皆非空；2014 年改制為直轄市原住民區的那些鄉在表中仍存在且帶生效迄。
- [x] 1.3 在 `data/sources.json` 登記該名單的來源，含出處 URL、取得日期、授權與性質，沿用專案既有的來源登記紀律。驗證：`data/sources.json` 可被 json 解析，且新增項目含非空的出處與授權欄位。

## 2. 逐屆清點來源資料的結構與可對應性

- [x] 2.1 清點 1998、2002、2005 三個獨立鄉鎮市長資料夾：記錄檔案清單、編碼、`elbase` 層級結構、選舉區命名方式，並嘗試用縣市＋鄉鎮市區代碼對應到 1.2 的名單，記錄成功與失敗筆數及失敗原因。依設計決策清點以「逐屆逐檔開啟並記錄」為準，不從單一屆推論其他屆，三屆分別記錄不合併結論，實現規格「Source availability is censused per term, and absence of a defect is distinguished from absence of checking」。驗證：清點報告中三屆各有獨立章節，各含對應成功／失敗筆數。
- [x] 2.2 清點 2009 合併檔（縣市長縣市議員及鄉鎮長）內的鄉鎮長部分，記錄同上項目，並額外記錄該屆行政區代碼與 2014 起是否互通。驗證：清點報告含該屆章節與代碼互通性的明確判定。
- [x] 2.3 清點 2014、2018、2022 地方公職人員選舉合併檔內的鄉鎮市長部分，記錄同上項目。驗證：清點報告含三屆各自章節與對應結果。
- [x] 2.4 釐清各鄉長補選資料夾涉及的鄉鎮分類（山地鄉／平地原住民鄉／非原住民鄉），以 1.2 的名單為判準，逐一記錄；目前僅蘭嶼鄉確認屬山地鄉，其餘未查證。驗證：清點報告列出每個補選資料夾及其分類判定，未能判定者明寫未判定。

## 3. 已知陷阱的三態判定

- [x] 3.1 對前述每一屆，逐一判定四類已知來源陷阱（尾隨空白、縣市代碼逐檔重編、鄉鎮市區代碼檔內重編、當選註記損壞）的狀態，依設計決策陷阱清點區分「確認不存在」與「未觀察到」，給出確認存在（附筆數與樣本）、確認不存在（附確認方法）或未檢查三者之一。驗證：清點報告中每一屆的每一類陷阱都有明確標示，無任何一格留白或省略。

## 4. 報告產出與文件更新

- [x] 4.1 產出 `docs/schema/山地鄉鄉長資料清點.md`，彙整第 2、3 群組的結果，並明確不下納入與否的結論，實現規格「The census does not decide inclusion and produces no derived figures」。驗證：報告內不含任何跨母體相除的數字；以人工複核確認全文沒有「應納入」「不應納入」這類結論句。
- [x] 4.2 更新 `README.md`「尚未解決」第 3 點，補上山地鄉鄉長的法源（地方制度法第 57 條第 2 項）與本次清點狀態，並指向清點報告。驗證：README 該點含法源條次與報告路徑。
- [x] 4.3 確認全專案既有測試維持通過，證明本次清點未動到任何建置路徑。驗證：`python scripts/test_build_site_data.py` 與 `python scripts/test_build_local_election.py` 皆回報全數通過，且 `git status` 顯示 `data/processed/` 與 `docs/` 下的 HTML 皆未被修改。
