## 1. 建置端新增欄位（必須先做完，站台端才能改）

- [x] 1.1 在長表建置腳本的候選人列組裝處新增 `年齡_有效`，緊接在 `年齡` 之後：`0` 在任何屆別都留空、`99` 只在具名的五個屆別（1994、1998、2002、2005、2006）留空、其餘放與 `年齡` 相同的值。實現 Sentinel-Bearing Columns Carry A Usable Derived Column 與 A Derived Column Holds Values, Not A Validity Flag，落實設計決策「新增 `年齡_有效`，放值或留空」與「不用布林旗標」。驗證方式：重建候選人長表後以指令讀出，斷言欄位數為 29、`年齡_有效` 位於 `年齡` 之後、五個舊屆全為空字串、2009-2010 以後四屆與 `年齡` 逐列相同。
- [x] 1.2 把兩條守住具名清單前提的斷言放進建置端，落實設計決策「判準與站台端一致，且只有一個實作」中「斷言隨判準一起移動」：具名為未記載的屆別出現第三種值（非 `0` 非 `99`）即中止並指出屆別與該值；未具名的屆別出現 `99` 即中止並指出屆別。驗證方式：以合成資料分別注入兩種情形，斷言各自中止且訊息含屆別；未注入時正常通過。
- [x] 1.3 在 oracle 清單新增 `年齡_有效` 條目，實現設計決策「`年齡_有效` 必須進 oracle 清單」：`provenance` 為 `project`，note 寫明判準、空字串的意義，以及「有效」指的是「有記載因而可用於計算」而非「這個年齡正確」——來源記載的年齡本身正確與否本專案未查證。驗證方式：執行既有的清單完整性檢查，確認通過；並以指令搜尋該條目，確認 note 含上述澄清。
- [x] 1.4 確認未覆寫來源值。驗證方式：逐列比對重建前後的 `年齡` 欄，斷言完全相同；並斷言 summary 與 votes 兩張長表的 SHA-256 與重建前相同。

## 2. 站台端改為讀取，移除重複實作

- [x] 2.1 站台產生器改為讀取 `年齡_有效`，刪除自己那份 `AGE_UNRECORDED_TERMS`、`AGE_ALWAYS_NO_DATA` 與 `display_age` 的推導邏輯，並把 `年齡_有效` 加入必要欄位宣告。實現 One Implementation Of A Sentinel Rule。驗證方式：以指令確認站台產生器原始碼不再出現 `AGE_UNRECORDED_TERMS`；欄位契約測試（宣告與實際讀取兩個方向）通過；站台 `--check` 通過且兩個 HTML 位元組不變。
- [x] 2.2 調整站台測試與變異測試以對應新結構：移除已搬走的判準測試，改為驗證「站台讀取 `年齡_有效` 而非自行推導」；建置端的兩條斷言各配一項變異。驗證方式：`pytest` 通過；站台變異測試腳本全部變異被偵測到且基準對照通過。

## 3. 文件

- [x] 3.1 在資料集說明文件的欄位表新增 `年齡_有效`，並在專案說明文件更新候選人長表的欄位數敘述。驗證方式：以指令搜尋兩份文件，確認 `年齡_有效` 出現且欄位數敘述為 29；並確認文件未把「有效」寫成對年齡正確性的背書。
