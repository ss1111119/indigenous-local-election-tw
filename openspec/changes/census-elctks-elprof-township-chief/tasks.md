## 一、清點數字實際來源的兩份檔案

- [x] 1.1 （實作 spec requirement「A census covers the files the figures come from before it is relied upon for inclusion」）逐屆開啟七屆（1998／2002／2005／2009-2010／2014／2018／2022）鄉鎮市長來源的 `elprof.csv`，記錄：欄位數、版面（合計在前或男女合計，以「男＋女＝合計」自我驗證分辨）、合計列位置、編碼、換行符、是否有欄位值前綴撇號、是否有尾隨空白（逐儲存格 `strip()` 比對並附筆數）。完成判準：`docs/schema/山地鄉鄉長資料清點.md` 第五節三態判定表中所有 `elprof` 相關列，七屆皆非「未檢查」。
- [x] 1.2 逐屆開啟同七屆的 `elctks.csv`，記錄：欄位數、最細行政層級（是否到村里或投開票所）、與同屆 `elbase` 的層級是否一致、是否存在 `elbase` 無對應列的孤兒層級（附筆數）。完成判準：三態判定表中所有 `elctks` 相關列七屆皆非「未檢查」。
- [x] 1.3 清點七屆的鄉鎮市區代碼是否在檔內重編（與同屆「區域」檔口徑比對），以及當選註記是否損壞（以 `elctks` 推導的當選人數與 `elcand` 的註記數比對，不符者附筆數）。填入三態判定表。任一屆發現損壞時具名該屆與筆數。

## 二、欄位語意的權威來源

- [x] 2.1 開啟 `data/raw/cec-votedata.zip` 內的官方格式文件，比對 `elcand` 第 14／15 欄語意，確認是否為「現任」與「當選註記」。將結論與所依據的文件檔名寫入清點文件，並把該項移出第七節「尚未清點」。若官方文件未涵蓋此欄，明文記為「官方文件未載明」——不得依樣本值（`Y`／`N`、`*`／空白）推斷。

## 三、可納入性結論

- [x] 3.1 （實作 spec requirement「Per-term includability is recorded as a conclusion separate from the inclusion decision」）依 1.1–2.1 的結果，在清點文件新增一節「可納入性結論」，逐屆記載「結構上可納入」或「結構上不可納入」及理由。該節須明文聲明此為技術判斷，不等同於「應否納入已發布資料集」的決定。
- [x] 3.2 在同一節列出後續納入工作應採用的逐屆預期山地鄉命中數。若 1.1–1.3 的清點結果與既有記載（1998／2002／2005 為 30、2009-2010 為 25、2014／2018／2022 為 24）不符，以本次實測為準並具名差異。
- [x] 3.3 執行 `git status` 確認本 change 只修改 `docs/schema/山地鄉鄉長資料清點.md`，`scripts/`、`data/processed/`、`docs/` 下 HTML 皆未變動。
