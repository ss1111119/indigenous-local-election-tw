## 1. 分桶改用身分對照表

- [x] 1.1 在站台資料產生腳本中建立 `PARTY_IDENTITY_BUCKETS`（鍵為 `(政黨代號, 政黨名稱)` 字串配對），初始五筆為 代號 1／中國國民黨、代號 2／民主進步黨、代號 16／民主進步黨、代號 99／無、代號 999／無黨籍及未經政黨推薦，未列入者歸「其他」；並把 `政黨代號` 加入 candidates 長表的必要欄位清單。實現 Party Buckets Are Keyed By Source Identity 與 Cross-Term Party Encoding Drift Is Named, Not Inferred，落實設計決策「分桶的鍵是 `(政黨代號, 政黨名稱)` 配對，寫成具名對照表」——對照表是具名條目，不得以名稱相似、子字串或共用代號推導（`無黨團結聯盟` 會被「含『無』」這類規則錯誤吸收）。驗證方式：以缺 `政黨代號` 欄的合成長表執行，斷言拋出缺欄例外而非 `KeyError`；並執行既有的欄位契約測試，確認宣告與實際讀取的欄位仍然互相吻合。
- [x] 1.2 把概況頁 `DATA` 的政黨累計改用該對照表分桶。驗證方式：產生常數後以指令讀出，斷言 1994／1998／2002／2005／2006 五屆的無黨籍候選人數分別為 14、44、50、42、1（修正前皆為 0），且各該屆「其他」桶的候選人數同額減少。
- [x] 1.3 執行既有的 `--check` 並逐項檢視差異，實現 A Silently Zeroed Series Is Treated As A Defect（那五屆的 0 是分類缺陷，不是「該屆沒有無黨籍參選」的事實）：確認出現差異的鍵**恰好**只有那五屆的無黨籍與其他兩個系列，沒有其他鍵被動到。驗證方式：把差異清單逐鍵列出並比對；若出現任何第三種鍵即停止並回報，不得逕行接受。

## 2. 名錄的分桶來源收斂

- [x] 2.1 名錄頁的 `MAIN` 改由腳本依同一份 `PARTY_IDENTITY_BUCKETS` 產生，以固定標記行就地替換寫入，找不到標記行即中止。實現 Bucket Membership Has A Single Source，落實設計決策「名錄的 `MAIN` 改為由腳本產生，不再手寫」。驗證方式：以移除標記行的暫時副本執行，斷言中止；正常執行後以指令讀出 `MAIN`，斷言它同時含有「無」與「無黨籍及未經政黨推薦」且兩者對應同一個色槽。
- [x] 2.2 以瀏覽器開啟名錄頁確認舊屆的無黨籍當選人徽章使用無黨籍的顏色而非「其他」的顏色。驗證方式：在 `#1998/T3` 與 `#2002/T3` 兩個檢視下檢查徽章的實際計算樣式，並確認 `#2022/T2` 的既有呈現未改變。

  ⚠️ 原文指定的 `#1998/T2` **驗不到**：徽章上色的 CSS 是 `li.won .pty[data-p=...]`，只有當選人的徽章有底色，而 1998 T2 有 14 位無黨籍候選人但 **0 位當選**。實測有舊屆無黨籍當選人的格子只有六個：1994 T-COMBO（1）、1998 T3（3）、2002 T2（2）、2002 T3（4）、2005 T2（7）、2005 T3（8）。已改用 `#1998/T3`。

## 3. 讓這類錯誤下次會被擋下

- [x] 3.1 新增「無黨籍逐屆非零」的測試，實現 The Independent Bucket Is Non-Empty In Every Term 與設計決策「未列入對照表的身分歸「其他」，不中止；改以具名斷言守住無黨籍」（已實測固定比例門檻與相對大小門檻皆不可用，故採具名領域斷言）：斷言九屆每一屆的無黨籍候選人數大於 0，失敗訊息須指出是哪一屆。驗證方式：`pytest` 通過；另以變異測試確認把 代號 99／無 這一筆從對照表移除時該測試會失敗、還原後通過。
- [x] 3.2 在站台的變異測試腳本新增三項變異並確認全部被偵測到：把分桶鍵改成只用名稱、只用代號、以及把同代號多名稱者自動合併。驗證方式：執行該腳本，斷言含新增項在內的所有變異皆被偵測到，且基準對照（未變異副本）通過。
- [x] 3.3 在建置輸出中列印「其他」桶逐屆的組成（成員名稱與候選人數，由多到少），落實設計決策「未列入對照表的身分歸「其他」，不中止；改以具名斷言守住無黨籍」中「讓下一個佔比異常的成員被人看見」，同時支撐設計決策「不新增第五桶」——「其他」不拆桶，改以組成列印讓它可被檢視。驗證方式：執行建置後檢查輸出含 2002 年「其他」桶以親民黨 28 位居首這一列。

## 4. 配色與文件

- [x] 4.1 把無黨籍改為中性深色、橘色不再指派給任何桶，落實設計決策「無黨籍改用中性深色，橘色不再指派給任何桶」。門檻與驗證形式服從既有能力 `site-chart-accessibility`（Categorical Colors Are Measured Against Each Other、Color Verification Is Recorded, Not Asserted），本任務不另訂標準。

  ⚠️ **必須先量「無黨籍（中性深色）↔ 其他（灰）」這一個配對。** 改色後這兩個桶是兩個中性色、只靠亮度區分，是整套配色裡最容易糊掉的組合，**不可目測**。

  ⚠️ **這個配對在系列順序上並不相鄰**，順序是 國民黨 → 無黨籍 → 民進黨 → 其他，無黨籍與其他是第 2 與第 4 位。**只跑預設的相鄰檢查會完全跳過它**，必須加 `--all-pairs`。

  工具為 in-repo 的 `scripts/palette_metrics.py`：

      python scripts/palette_metrics.py "#3987e5,<新的中性深色>,#1da77a,#6A7178" --labels 國民黨,無黨籍,民進黨,其他 --all-pairs

  驗證方式：明、暗兩個主題各跑一次 `--all-pairs`，斷言全部配對在正常視覺 ΔE ≥ 15、protan 與 deutan 模擬 ΔE ≥ 8（工具會自行判定並以非零退出碼表示未過）；未達門檻即重新調色，不得放行。無黨籍↔其他 這一組的實測值必須逐一列出，不可只寫「通過」。另須跑 `scripts/test_site_invariants.py` 的 `test_series_palette_separation`。
- [x] 4.2 把 4.1 所用的工具名稱、模擬的色覺障礙類型、ΔE 門檻與實際量到的值（含無黨籍↔其他 這一組）寫進專案說明文件，取代現有那句無法查核的「配色以 CVD 驗證器檢過」。

  ⚠️ 類型只能寫 **protan／deutan**，**不可寫 tritan**——in-repo 工具未實作 tritan（Viénot 單平面與 Machado 矩陣都與外部驗證器對不上，差 4~18）。專案 spec 的 CVD 門檻本來也只涵蓋 protan／deutan。若要宣稱 tritan，必須另以外部工具量測並標明來源。

  驗證方式：以指令搜尋該句，確認已被含有工具、類型、門檻與實測值的敘述取代，且全文不出現未經量測的 tritan 宣稱。
- [x] 4.3 落實設計決策「兩份主 spec 的 Purpose 一併補上」：把兩份主 spec 的 `## Purpose` 由歸檔工具留下的 `TBD - created by archiving change ...` 佔位文字改寫為實質內容。驗證方式：以指令搜尋 `openspec/specs/` 下的 `TBD - created by archiving`，確認 `site-data-generation` 與 `legacy-source-quirks` 兩份不再命中。
