# HANDOFF — 交接狀態

最後更新：2026-08-20。**這份檔案是給下一個接手的人／模型看的，不是專案說明。**
專案本身的說明在 `README.md`；本檔只記「現在停在哪裡、下一步做什麼、有哪些會靜默出錯的地雷」。

---

## 一、現在停在哪裡

### 已完成並提交

- `b6fda7c spectra(include-1994-2006-terms): 納入 1994-2006 舊屆別並防堵來源靜默錯誤`
- `2599c73 docs: 文件同步至九屆，並補上 當選／elected_authoritative 的分工警告`
- `9bdaf25 spectra(update-site-to-nine-terms): 站台前端擴充至九屆，常數改由腳本產生`
- `82d9a17 spectra(update-site-to-nine-terms): 歸檔，並建立 site-data-generation 主 spec`

**資料集現在涵蓋九屆**：1994、1998、2002、2005、2006、2009-2010、2014、2018、2022。
Spectra change `include-1994-2006-terms` 10/10 完成，**已歸檔**至
`openspec/changes/archive/2026-08-20-include-1994-2006-terms/`，
並建立本專案第一份主 specs（`openspec/specs/` 下兩個能力、9 條 Requirement）。

**站台已跟上九屆。** change `update-site-to-nine-terms` 10/10 完成、**已歸檔**至
`openspec/changes/archive/2026-08-20-update-site-to-nine-terms/`，
並新增第三份主 spec `site-data-generation`（5 條 Requirement）。

**目前沒有進行中的 change。** `openspec/specs/` 下三個能力、14 條 Requirement。

議員席次序列（權威值）：

| 屆 | T2 平原 | T3 山原 | 自訂代碼（不進主序列） |
| --- | ---: | ---: | --- |
| 1994 | — | — | 省議員平原 2、山原 2、直轄市合併 2 |
| 1998 | 23 | 30 | 直轄市合併 2 |
| 2002 | 26 | 30 | 直轄市合併 2 |
| 2005 | 27 | 30 | — |
| 2006 | — | — | 直轄市合併 2 |
| 2009-2010 | 31 | 32 | — |
| 2014 | 33 | 33 | — |
| 2018 | 34 | 35 | — |
| 2022 | 34 | 35 | — |

---

## 二、下一步（依優先序）

1. **`docs/schema/cec-local-election.md` 的「18 項自我驗證」列表未隨新驗證更新。**
   數字與內容都已落後，讀者會據此以為某些檢查存在而其實不存在。
2. **鄉鎮市區層級的跨檔正規化**（另案）。見下方地雷 2。
3. **`data/processed/cec-county-code-crosswalk-1998-2002.csv` 放錯位置**——
   它是**輸入**（人工建立的對照表，`build_local_election.py:543` 由 `OUT_DIR` 讀入），
   不是建置產物，卻和產物放在同一個資料夾。
   「清空輸出目錄再重跑」這個很自然的動作會把它一起刪掉，然後建置失敗。
   它有入版控，`git restore` 救得回來，不會永久遺失——但值得搬到 `data/` 下另一處。

---

## 三、會靜默出錯的地雷

**「靜默」的意思是：不會報錯，數字看起來完全合理。**
八項具名瑕疵完整記錄在 `data/sources.json` 的
`cec-votedata.legacyTerms1994_2006.knownDefects`，處置與補償性檢查見
`include-1994-2006-terms/design.md`。這裡只列**還會咬到下一個人**的。

### 1. `當選` 欄反映來源錯誤，要席次請用 `elected_authoritative`

`當選` 由 `elcand` 註記推導。2005 山原只算出 18 席（正確 30）、平原 20（正確 27）；
1994 高雄市標錯人。63 位候選人已逐一具名於 `KNOWN_ELECTED_MARK_ANOMALIES`。

### 2. 鄉鎮市區代碼在 1998／2002／2005 是**檔內重編**，本專案未正規化

`鄉鎮市區_正規化` 對這些檔**留空**——空字串代表「未正規化」，不是資料缺漏。
刻意不放原始碼：放了會讓 `(縣市, 鄉鎮市區)` 的 join 成功執行但**對錯行政區**。

⚠️ **「2005 起才是全域代碼」只在縣市層級成立。**

⚠️ 若要做鄉鎮市區層級對照，**2002 山原檔有三個名稱被截斷**（阿里山鄉→`里山鄉`、
三地門鄉→`地門鄉`、太麻里鄉→`麻里鄉`），而那三個正是山地原住民的核心行政區。
規則必須是綁死（屆別, 選舉種類, 縣市, 原始名稱）的具名 alias，
**不可**用「去掉首字比對」或「後 N 字相等」這類通則。

### 3. 有一項補償性檢查是**必要條件而非充分條件**

2005 兩檔 3＋4 個選舉區、1998 平原 2 個選舉區的鄉鎮市區有效票被配錯位置。
補償檢查是「多重集合相同」——但若 B 鄉與 C 鄉的票數**互換**，多重集合仍相同、
檢查會放行。實測錯置沒有系統性規律，且來源沒有第三個獨立錨點可判斷哪邊才對。
**這些選舉區的鄉鎮市區層級數字，其「歸屬哪個鄉鎮市區」不可驗證。** 已記在 design。

### 3b. 站台兩份 HTML 的資料常數是**產生**的，不要手改

`docs/index.html` 的 `DATA` 與 `docs/roster.html` 的 `D` 由
`python scripts/build_site_data.py --write` 產生，只替換標記行、其餘位元組不變。
手改會在下次 `--write` 被蓋掉，而且 `--check` 會開始報未預期差異。
**先前這兩個常數是手動維護的，那正是站台落後資料集四屆的原因。**

⚠️ 真正的「主序列過濾」在 `docs/index.html` 的
`DATA.types.filter(t => t.mainSequence)`，**不在 Python 端**。
Python 只負責把旗標算出來。刪掉前端那一行，Python 測試會全綠、
站台卻已經把三個自訂選舉種類畫進跨屆折線——
`scripts/test_build_site_data.py` 用 node 直接執行那兩行來守這一側。

### 3c. 缺屆的佔位是「無此選舉」與「—」，**不是 `×`**

先前文件多處寫 `×`，站台實際從未渲染過那個符號。折線圖是留空。

### 4. 複合鍵一律用 `|` 分隔，不要用連字號

自訂選舉種類代碼含連字號（`T-COMBO`／`T-PRV2`／`T-PRV3`），屆別也含（`2009-2010`）。
`"1994-T-COMBO".rsplit("-", 1)` 會切成 `("1994-T", "COMBO")`。
`validation-report.json` 的 `各屆別選舉種類全國合計` 與測試的釘死值都已改用 `|`。

### 5. 驗證 4／7 檢查的是**來源**，權威值另立 4c／4d

不要為了讓 2005 通過而把第 4 項改用權威值——那會讓它再也偵測不到 `elcand` 損壞。
兩次外部覆核都明確反對那個選項。

---

## 四、事實更正（先前文件寫錯過的）

1. **1994–2006 是 21 個 leaf 資料夾、清點 17 個**（13 原住民檔＋4 同屆區域對照檔）。
   先前記載的「19 個檔」與任何算法都對不上。
2. **2005 屏東縣第十六選區不是「同額競選未投票」。** `elprof` 寫投票數 1,927，
   `elctks` 也有該候選人的列（在鄉鎮市區層級、得票 1,794、註記 `*`），
   只是 `elcand` 的鄉鎮市區欄是 `000` 所以「代碼整組相等」的鍵對不上。
   它就是註記損壞的其中一筆。**因此不需要「elctks 無列時由 elprof 補齊」的回退**，
   而且不該有——實測沒有任何候選人缺 elctks 列，該回退會在 elctks 整區遺失時
   把整區靜默標成當選。
3. **README 的列數表在 2026-08-20 之前就已經錯了**（寫 204,617/5,754/1,332,591，
   四屆版本實際是 266,336/7,335/1,746,316），至少落後兩輪。已更正為九屆數字。
4. **本 repo 不計算「人口比例 vs 席次比例」。** `docs/index.html` 裡「人口」出現 0 次。
   那是隔壁專案 `indigenous-constitution-tw` 的核心指標。
5. **席次跳點的成因是職位不同，不是同一職位斷裂。** 2009-2010 的 63 → 2014 的 190
   來自 D2／R3／R2 在 2014 才納入。現有站台每個圖表都已按選舉種類分開呈現。

---

## 五、工作紀律（違反過才寫進來的）

- **不要 `git add -A`。** 使用者與 AI 共用同一個工作目錄並會並行提交，一律明確列路徑。
  目前 `.gitignore` 有使用者的未提交改動，不要碰。
- **不覆寫來源數字。** 既有處置模式是「原樣保留＋具名記錄＋補償性檢查」。
  外部覆核曾建議寫硬編碼修補直接覆寫，**未採納**。
- **具名到最細的粒度。** 整檔放行等於在該檔停掉那項驗證。
  當選註記具名到候選人、鄉鎮市區錯置具名到選舉區、選舉區欄不一致連
  **允許值**都逐檔釘死。
- **不下未清點的全稱宣稱。** 要寫「不存在」「全部都是」之前先跑一次計數。
- **驗證要能失敗。** 每加一條驗證，配一個變異測試：改壞必須中止、還原必須通過。
  兩支腳本**已入版控**：`scripts/mutate_build_local_election.py`（48 個變異，
  **目前 10 項未被偵測到**）與 `scripts/mutate_build_site_data.py`（17 個，全數偵測到）。

  ⚠️ 那 10 項全是「把某條驗證拿掉」型，共同點是**那些驗證在乾淨資料上永遠不觸發**，
  拿掉了輸出也不變。基準對照是乾淨的（未變異通過、無測試被跳過），所以是真的漏網。
  補法與站台那邊相同：為那些驗證各寫一組會觸發它的合成輸入。
  **在補上之前，不要宣稱那 48 條驗證受測。**

  兩支都在副本上變異，不動真正的
  `scripts/`；唯一的例外是 `docs/index.html` 那一項（測試用 node 讀真檔），
  它會先要求該檔在 git 下乾淨，還原用 `git checkout --` 而非記憶體裡的備份。

  ⚠️ **副本必須放在 repo 根目錄正下方一層（`_mut/`），與 `scripts/` 同深度。**
  測試檔以 `__file__.parent.parent` 推導 ROOT。原本放在 `scratch/mut/`（兩層），
  ROOT 解析成 `scratch/`，於是 `data/processed` 與原始壓縮檔都找不到，
  **迴歸／舊屆／自訂種類三項測試全部靜默跳過、pytest 仍報 passed**。
  那段期間「48 個變異全部被偵測到」完全是靠單元測試達成的。

  ⚠️ **變異腳本的檔名不可用 `test_*.py` 或 `*_test.py`，執行部分要包進 `main()`。**
  原名 `mutation_test.py` 搬進 `scripts/` 後，pytest 依預設樣式收集它，
  **在收集階段就把整套變異跑完**（實測 52 秒）才以 INTERNALERROR 收場。

  ⚠️ 有**七次**是變異測試先漏網、才發現測試無效——不要跳過這一步。
  最近一次：投票率的捨入測試只挑了 52.625，而 `ROUND_CEILING` 在那個值上
  與 `ROUND_HALF_UP` 同解，把捨入模式改壞不會被抓到。
  改用 33.333…／66.666… 才同時卡住一律進位與一律捨去。
  ⚠️ 站台那支變異測試**在 pytest 下執行**而非直接執行，
  因為這個專案正是在那個落差上出過事（見上面 `@reports`）。
- **註解不可宣稱程式沒做的事。** 外部覆核抓到 `KNOWN_ELECTOR_ANOMALIES` 的
  「上層級必須正常」只寫在註解裡而沒有實作。
- **外部覆核（`codex` MCP／`agy` CLI）送出前，先逐條查證提問裡的事實宣稱。**
  它們抓到的方向常對，細節必須自己驗——這一輪 agy 就有一項宣稱經實測不成立
  （「全面 strip 會讓逐位元組驗收失敗」，實際三張表雜湊相同）。
- **改 `.py` 用編輯工具，不要用 heredoc 跑 Python 改檔**——替換字串裡的 `\n`
  會變成真正的換行，寫出語法錯誤的檔案。**累計踩了八次以上**，
  最後一次是在 f-string 裡，比純字串更難一眼看出。這條沒有值得冒險的例外。
