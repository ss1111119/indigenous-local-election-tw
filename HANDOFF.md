# HANDOFF — 交接狀態

最後更新：2026-08-21。**這份檔案是給下一個接手的人／模型看的，不是專案說明。**
專案本身的說明在 `README.md`；本檔只記「現在停在哪裡、下一步做什麼、有哪些會靜默出錯的地雷」。

---

## 一、現在停在哪裡

**最後更新：2026-08-25。**

### 資料集

| 資料集 | 屆別 | 輸出 |
| --- | --- | --- |
| 地方公職（議員、區長、代表） | 1994–2022 九屆 | `cec-local-election-*` 三張長表 |
| 原住民立法委員 | 1995–2024 九屆 | `cec-legislative-election-*` 三張長表 |
| 不分區政黨票的界限估計 | 2008–2024 五屆 | `indigenous-party-preference-bounds.csv` |

⚠️ **三者是三個不同的母體，不可互相比較，也不可相加。** 見地雷 1c。

### 已歸檔的 change（11 個）

`fix-party-bucket-drift`、`include-1994-2006-terms`、`update-site-to-nine-terms`、
`add-indigenous-legislative-elections`、`age-99-is-unrecorded`、
`candidate-age-valid-column`、`elected-column-swap`、
`normalise-town-codes-1998-2005`、`site-accessibility-baseline`、
`test-unguarded-source-checks`、`add-party-list-votes`。

全部在 `openspec/changes/archive/`，目錄名帶歸檔日期。

### 主 specs：12 個能力、**78 條 Requirement**

| 能力 | 條數 |
| --- | ---: |
| `legacy-source-quirks` | 22 |
| `site-data-generation` | 9 |
| `site-chart-accessibility` | 8 |
| `indigenous-legislative-elections` | 7 |
| `party-list-votes` | 5 |
| `bounded-estimates` | 4 |
| `election-period-publication` | 4 |
| `historical-terms-1994-2006` | 4 |
| `site-translation` | 4 |
| `site-heading-segmentation` | 4 |
| `column-oracle-documentation` | 4 |
| `site-multi-dataset` | 3 |

### 已歸檔的 change（續）

`site-legislative-and-party-preference`（11/11）與 `election-period-publication`
（10/10）於 2026-08-23 歸檔，`site-english-pages`（12/12）與
`site-chart-interactivity`（8/8）於 2026-08-24 歸檔，`budoux-heading-wrap`
（10/10，新增 `site-heading-segmentation` 能力：建置期用 BudouX 替
`docs/index.html`、`docs/legislative.html` 的靜態中文標題插入語意斷詞點）、
`legislative-oracle-doc-and-population-check`（6/6，新增
`column-oracle-documentation` 能力：立委的欄位 oracle 對外曝光＋人口數
驗證）、`population-decimal-hardening-and-atomic-oracle-write`（5/5，
強化人口數驗證邊界情況＋oracle 文件原子寫入）與 `party-list-oracle-doc`
（2/2，補上政黨票長表同一類的 oracle 對外曝光缺口）於 2026-08-25 歸檔。
**目前沒有進行中的 change。**

議員席次序列（權威值）：

| 屆 | T2 平原 | T3 山原 | 自訂代碼（不進主序列） |
| --- | ---: | ---: | --- |
| 1994 | — | — | 省議員平原 2、山原 2、直轄市合併 2 |
| 1998 | 23 | 30 | 直轄市合併 2 |
| 2002 | 26 | 30 | 直轄市合併 2 |
| 2005 | 27 | 30 | — |
| 2006 | — | — | 直轄市合併 2 |
| 2009-2010 | 31 | 32 | — |
| 2014 | 33 | 33 | — |
| 2018 | 34 | 35 | — |
| 2022 | 34 | 35 | — |

原住民立委席次（山地＋平地合計）：1995 起 6／8／8／8／6／6／6／6／6。

---

## 二、下一步（依優先序）

**目前沒有待辦的工程項目。** `site-legislative-and-party-preference` 已於
2026-08-23 完成 11/11 並歸檔，兩件懸而未決的取捨也已做成決定（見下）。

唯一還開著的是 `README.md`「尚未解決」第 2 點：**地方層級的代表性指標未定義**。
各縣市席次、選區與選舉人範圍不同，全國加總後除以席次會掩蓋地方差異。
⚠️ **立委頁不是它的答案**——立委是全國選舉區，地方層級算不出同一個東西。

### 2026-08-23 做成的兩個決定（不要重開）

**repo 不改名。** `local` 確實已不足以描述涵蓋範圍，但名稱不準確的唯一實害是
讀者誤判範圍，那已由 repo description 與 README 開頭的資料集表修正，代價為零。
改名的代價是 **GitHub Pages 網址失效**——git remote 有永久轉址、**Pages 沒有**，
已流出的連結收不回來。若日後仍要改：`README.md` 6 處、`docs/sitemap.xml` 3 處、
`docs/三屆概況.md` 1 處，相鄰三個 repo 零引用。
⚠️ **本機資料夾不可跟著改名**：工具的記憶目錄以本機路徑為鍵。

**`縣市`／`鄉鎮市區` 不比照 `當選`／`年齡` 對調。** 那兩次對調的理由是
「天真的用法會得到錯的答案」（`AVG(年齡)` 會吃到 `99` 哨兵、數來源註記在 2005
兩檔少 19 席）。縣市碼不是那種情況：它在**同一屆檔內就是正確的識別碼**，
只有跨屆比對需要正規化值，而那是刻意的動作。另外 `鄉鎮市區_正規化` 只有地方公職
才有，對調會讓它與立委的欄位語意更加分歧；而換語意的代價本專案付過——
`當選` 那次的漏網處在 diff 裡一個字都沒改，只是讀的欄位語意翻了，38 項測試全綠。

---

## 三、會靜默出錯的地雷

**「靜默」的意思是：不會報錯，數字看起來完全合理。**
八項具名瑕疵完整記錄在 `data/sources.json` 的
`cec-votedata.legacyTerms1994_2006.knownDefects`，處置與補償性檢查見
`include-1994-2006-terms/design.md`。這裡只列**還會咬到下一個人**的。

### 1. `當選` 的語意在 2026-08-21 換過，且**欄位數由 29 變 28**

`當選` 現在存的是**跨檔比對後的權威值**（`Y`／`N`），來源的認定退居 `當選註記`（原樣）
與 `當選註記語意`（解碼）。`elected_authoritative` 欄已移除，
`elected_authoritative_basis` 更名為 `當選_依據`。

⚠️ **兩個靜默陷阱：**

1. **同名欄位、不同語意。** 更早的修訂版中 `當選` 存的是**來源認定**，
   那個版本的 2005 山原只算得出 18 席（正確 30）、平原 20（正確 27）。
   拿舊版產物跟新版比對會看到席次「暴增」，那不是資料變了，是欄位語意變了。
2. **不可用位置索引取值。** 移除一欄會使其後所有欄位左移一格，
   依位置讀取的下游不會收到任何錯誤訊息，只會拿到下一欄的字串。

來源損壞本身沒有被覆寫：1994 高雄市標錯人、2005 兩檔少標 19 席，
63 位候選人仍逐一具名於 `KNOWN_ELECTED_MARK_ANOMALIES`，
補償檢查比對的是「由 `當選註記` 現算的值 vs `當選`」。
⚠️ 那個檢查**必須**維持這個比對方向——若兩邊都改讀權威值就成了恆真同義反覆、
靜默失效。變異 49 專門守這一點。

### 1b. 立委的三套縣市代碼——**2012 那屆與其他兩期都不同**

原住民立委長表涵蓋 1995-2024，縣市代碼有三套系統：

| 縣市 | 1995–2008 | 2012 | 2016+ |
| --- | --- | --- | --- |
| 宜蘭縣 | 03, **002** | 06, **001** | 10, **002** |
| 臺北縣／新北市 | 03, **001** | 已升格改名 | 65, 000 |

⚠️ **`001` 在 2012 是宜蘭縣、在 1995 是臺北縣。** 而 1995-2008 與 2016-2024 的
編號**恰好相同**——只有 2012 整組重編。這使得「抽兩屆比對就以為安全」特別
容易發生。實測 `02,000` 在 1995-2008 是高雄市、在 2012 是新北市。

跨屆比對用 `縣市_正規化`，或先看 `admin_code_system` 欄。
鄉鎮市區同理用 `鄉鎮市區_正規化`（秀林鄉的碼 1995 是 011、2004 是 003、
2020 是 110）。⚠️ 這是**地方公職**的欄；立委那支沒有鄉鎮市區正規化。

### 1c. 立委的年齡哨兵屆別與地方公職**不同**

立委是 1995／1998／2001／2004，地方公職是 1994／1998／2002／2005／2006——
**只有 1998 重疊**。`build_legislative_election.py` 有自己的
`AGE_UNRECORDED_TERMS`，**不要 import 地方公職那一份**。

### 1d. 站台上的三個資料集**分開呈現是刻意的**，不是還沒整合

`index.html`／`roster.html` 是地方公職，`legislative.html` 是原住民立委，
該頁第四節是不分區政黨票的界限估計。合併呈現會產生三種錯誤，且都不會報錯：

1. **母體不同。** 地方公職是各縣市選舉區，立委是山地／平地兩個全國選舉區。
   兩邊的投票率放進同一條折線，讀者會讀成同一群人的行為變化。
2. **分桶集合不同。** 立委頁有五個政黨桶，地方公職三個。共用一套會讓
   親民黨 2001 年的 27.7%、無黨團結聯盟 2004 年的 26.0% 掉進「其他」——
   而那正是那兩屆的主要故事。`check_bucket_sets_differ()` 專門擋這件事。
3. **涵蓋率不同。** 前兩者是全查；第三個最嚴的門檻只涵蓋 11.0% 的原住民
   選舉人且全在山地鄉。把它跟全查的數字並排，等於宣稱它適用於全體。

⚠️ 界限那一節的限定語與數字**必須在同一個可複製的區塊內**。讀者複製一段
貼到別處時，限定語要跟著走；放在頁尾註腳等於沒有。

### 1e. **現在在選舉期間內**，發布規則預設從嚴且判定紀錄是機器驗的

2026 年地方公職人員選舉：選舉公告 **2026-08-20** 發布、投票日 **2026-11-28**、
公告當選人名單 **2026-12-04** 前（中選會第 622 次委員會議）。
本專案的階段界線取**選舉公告發布日**，不取法定競選活動期間（11-13 起）——
兩者差三個月，取寬的等於這條規則在最需要它的三個月裡不生效。

⚠️ **選舉期間不得新增解讀性指標。** 判準是兩問，兩問皆是才算：
(1) 不是全查（需推估或跨母體相除）、(2) 帶方向（落差／傾向／不足）。
得票率、席次、投票率 → 歷史數據，可以動。政黨傾向界限 → 解讀性指標，**已凍結**。
⚠️ 「每個輸入都來自官方檔案」**不構成**歸為歷史數據的理由——
估計值的輸入本來就都是官方的。

⚠️ **凍結的意思是形狀不長大，不只是數字不更新。** 政黨傾向界限宣告為
5 屆 × 3 門檻，`check_frozen_indicator_shape()` 比對實際值；多一屆、多一個門檻
都會中止，**即使既有的每個數字都沒變**。解凍條件是 2026-12-04 之後。

⚠️ **`docs/` 下每加一個 HTML，就必須在 `docs/發布判定紀錄.md` 裡加一列。**
`check_publication_record()` 兩個方向驗（漏列、列了不存在的頁面），
`--check` 與 `--write` 都會跑，漏了就中止並具名該檔。
要擋的不是「判定寫錯」，是「多了一頁，沒有人想起要判定」。

⚠️ **本屆限定語的檢查比對 `CURRENT_TERM_NOTICE` 這個具名字串，不可比對「2026」。**
頁尾本來就有 `更新：2026-08`，用年份當判準的檢查在標示被刪掉後照樣通過。

### 1f. 站台的限定語由 `STRINGS` 產生，**兩版不可各自手寫**

`docs/en/` 下有 index 與 legislative 兩個英文頁（名錄沒有，理由見 README）。
中英兩版的**資料常數逐鍵完全相同**，只有 `T`（文案）與 `L`（標籤）不同，
兩者都由 `scripts/build_site_data.py` 的 `STRINGS` 與 `LABELS_EN` 產生。

⚠️ **兩版的 JS 完全相同。** 收 UI 文字進 `STRINGS` 就是為了這件事——
兩份各自演化的繪圖程式碼是本專案在名錄 `MAIN` 上踩過的坑。
改圖表要改兩個檔，但改的內容必須一模一樣；只改一邊不會有東西報錯。

⚠️ **靜態 HTML 裡的限定語不走 `T`。** 交給 JS 注入會讓「沒有 JS 就沒有限定語」。
它們留在 HTML 原始碼裡，由 `check_static_qualifiers()` 斷言與 `STRINGS` 逐字相同。

⚠️ **涵蓋檢查必須 `rglob`，不是 `glob`。** `docs/en/` 是子目錄；非遞迴會讓那兩頁
被靜默跳過。判定紀錄的鍵也因此改用**相對路徑**——`index.html` 與 `en/index.html`
檔名相同，用檔名兩者會互相掩護。

⚠️ **限定語的檢查要排除 `<script>` 區塊。** 限定語同時存在於頁面的 `const T`
常數裡，grep 整個檔案的話，把讀者看得到的那份刪掉也照樣通過——量到的是 `T`，
不是頁面上的字。實測就是這樣漏掉的。

⚠️ **`LABELS_EN` 的每一項都要標出處**（`cec`／`common`／`project`）。
中選會英文站是 JS 渲染的、`WebFetch` 讀不到本文，查不到官方用法的一律標 `project`，
不可標 `cec` 充數。界限完整表格裡 36 個沒有通行英文名的政黨**保留中文原名**。

### 1g. 圖表的 `bind()` 是 hover 與鍵盤 focus 共用的同一段邏輯

`docs/index.html`／`docs/legislative.html`（含各自英文版）的 `bind(el, text)`
同時監聽 `pointerenter`/`focus` 顯示、`pointerleave`/`blur` 隱藏。
**未來加新圖表要沿用這個函式，不要另寫一套鍵盤專用的實作**——本專案已經
因為「同一規則兩處實作」出過兩次 bug（名錄 `MAIN` 手寫、政黨分桶只認一種名稱）。

⚠️ `index.html` 的投票率圖與政黨席次圖，資料點／區段包在原生 `<a href="roster.html#屆別/代碼">`
裡（英文頁固定指向 `../roster.html`）——`roster.html` 早已支援這個雜湊定位。
**立委頁刻意不加這個導航**：`L2`／`L3` 沒有對應的名錄頁可以連過去。

⚠️ **互動性的安全邊界是「篩選、定位、可及性」，不能讓讀者在前端自選欄位算比值**——
即使算式跑在瀏覽器端、沒有寫死進 HTML，一樣算是在選舉期間發布了新的解讀性指標。
2026-08-24 與 Codex、Antigravity 分別討論後的共同結論。

### 1h. 本專案第一個第三方 Python 依賴：`requirements.txt` 的 `budoux`

`docs/index.html`、`docs/legislative.html` 的靜態中文 `<h1>`／`<h2>` 由
`build_site_data.py` 的 `segment_headings()` 在 `--write` 時插入 BudouX
語意斷詞的 `<wbr>`，避免窄螢幕把「投票率」「地方」這類詞從中間切開。
**跑 `--write`／`--check` 之前要先 `pip install -r requirements.txt`。**

⚠️ 只有這兩個檔的 `<h1>`／`<h2>` 在範圍內（見 `ZH_HEADING_PAGES`）。
英文頁原生依空白斷行不需要；`roster.html` 的 `<h2>` 是 JS 在瀏覽器端
用縣市名稱動態產生，縣市名固定 2-4 字不會有這問題，也不是建置期靜態文字。

⚠️ **只插 `<wbr>` 不夠**——CJK 文字瀏覽器預設每個字之間都能斷行，`<wbr>`
只是多一個選項，不會蓋掉預設規則。兩個檔各自的 `<style>` 都在 `h1{...}`／
`h2{...}` 規則加了 `word-break:keep-all` 與 `overflow-wrap:anywhere`，
斷詞點才真正生效。少了這兩個 CSS 屬性，語意片段照樣會在窄螢幕被切開，
且純文字比對測不出來（`test_heading_segmentation_preserves_visible_text`
只驗字沒少沒多，不驗有沒有生效）。

### 1i. `spectra task done` 記的「touched files」是掃當下整個 git status，不是只抓這個任務真正碰過的檔案

實測驗證過（丟一個假任務進假 change，只標記完成、不做任何真正的編輯）：
`.spectra/touched/<change>.json` 記進了 130 幾個檔案，包含 `scratch/`
底下一堆探索性腳本——這些檔案跟那個假任務完全無關，只是剛好那時候整個
工作目錄裡全部處於未追蹤狀態。

⚠️ **這會污染 `spectra archive` 產生的 `@trace` 註解**：`code:` 清單抓的
就是這份被污染的 touched.json，2026-08-25 archive 過的三個 change
（`budoux-heading-wrap`、`legislative-oracle-doc-and-population-check`、
`population-decimal-hardening-and-atomic-oracle-write`）的 `@trace`
裡全部混進了無關的 `scratch/*.py`。只影響 HTML 註解形式的溯源 metadata，
不影響任何實際建置或測試行為。

**已處理**：`scratch/` 加進 `.gitignore`（2026-08-25），從此不會再被
`git status` 掃到。若之後又看到 `@trace` 裡出現不相干的檔案，先檢查
工作目錄乾不乾淨（`git status --short`），不要假設是別的邏輯錯誤。

### 2. 鄉鎮市區代碼在 1998／2002／2005 是**檔內重編**，已於 2026-08-22 正規化

`鄉鎮市區_正規化` 對這六個檔已換算為同屆「區域」檔口徑
（1,290 個單位，829 個換算後與原碼不同）。**跨檔比對一律用這一欄。**
`鄉鎮市區` 欄保留來源原值，直接拿它 join 會成功執行但**對錯行政區**。

⚠️ **「2005 起才是全域代碼」只在縣市層級成立。**

⚠️ **四筆名稱截斷是具名 alias，不是通則，改成通則會靜默錯配。**
2002 山原：阿里山鄉→`里山鄉`、三地門鄉→`地門鄉`、太麻里鄉→`麻里鄉`；
**2005 山原另有一筆**：太麻里鄉→`麻里鄉`。

成因不一致，這是關鍵：2002 山原整份檔的鄉鎮名稱最長就是 3 字（三個四字名
全被截），但 2005 山原保留了三地門鄉與阿里山鄉，**只截太麻里鄉**。
所以「長度超過三字」在 2005 不成立；而「去掉開頭一字」會在平原檔那 5 個
合法的兩字名（東區、南區之類）上製造偽陽性。

對照表由 `python scripts/build_town_crosswalk.py` 產生後入版控，但**建置每次
都由來源重新推導再逐列比對**——對照表與來源脫節即中止，不沿用舊值。
alias 只在那條重新推導的路徑上才會被檢查；改成只查表的話，
alias 宣告錯了也不會有任何反應。

### 3. 有一項補償性檢查是**必要條件而非充分條件**

2005 兩檔 3＋4 個選舉區、1998 平原 2 個選舉區的鄉鎮市區有效票被配錯位置。
補償檢查是「多重集合相同」——但若 B 鄉與 C 鄉的票數**互換**，多重集合仍相同、
檢查會放行。實測錯置沒有系統性規律，且來源沒有第三個獨立錨點可判斷哪邊才對。
**這些選舉區的鄉鎮市區層級數字，其「歸屬哪個鄉鎮市區」不可驗證。** 已記在 design。

### 3b. 站台兩份 HTML 的資料常數是**產生**的，不要手改

`docs/index.html` 的 `DATA` 與 `docs/roster.html` 的 `D` 由
`python scripts/build_site_data.py --write` 產生，只替換標記行、其餘位元組不變。
手改會在下次 `--write` 被蓋掉，而且 `--check` 會開始報未預期差異。
**先前這兩個常數是手動維護的，那正是站台落後資料集四屆的原因。**

⚠️ 真正的「主序列過濾」在 `docs/index.html` 的
`DATA.types.filter(t => t.mainSequence)`，**不在 Python 端**。
Python 只負責把旗標算出來。刪掉前端那一行，Python 測試會全綠、
站台卻已經把三個自訂選舉種類畫進跨屆折線——
`scripts/test_build_site_data.py` 用 node 直接執行那兩行來守這一側。

### 3c. 缺屆的佔位是「無此選舉」與「—」，**不是 `×`**

先前文件多處寫 `×`，站台實際從未渲染過那個符號。折線圖是留空。

### 4. 複合鍵一律用 `|` 分隔，不要用連字號

自訂選舉種類代碼含連字號（`T-COMBO`／`T-PRV2`／`T-PRV3`），屆別也含（`2009-2010`）。
`"1994-T-COMBO".rsplit("-", 1)` 會切成 `("1994-T", "COMBO")`。
`validation-report.json` 的 `各屆別選舉種類全國合計` 與測試的釘死值都已改用 `|`。

### 4b. 對照表放在 `data/reference/`，不要搬回 `data/processed/`

`cec-county-code-crosswalk-1998-2002.csv` 是**人工建立的輸入**，不是建置產物。
2026-08-22 由 `data/processed/` 搬到 `data/reference/`（立委那支從一開始就在那裡）。

⚠️ 搬回去之後**建置照樣成功、輸出逐位元相同**——會壞的是「清空輸出目錄
再重跑」這個動作，而那不在任何測試的路徑上。因此明寫了一條位置檢查
（`test_county_crosswalk` 內）與對應變異，否則沒有東西擋得住它被改回去。

### 4c. 政黨代號跨屆會漂移，分桶鍵一定要含名稱

不分區政黨票的 `elpaty` 五屆合計 390 個代號，其中 **9 個在不同屆對到不同名稱**
（79／102／133／134／166／189／195／199／278）。例如 133 是
紅黨 → 台灣國民會議 → 台灣人權聯盟。

⚠️ **機制有兩種而本資料判不出是哪一種**：同一政黨改名，或代號被另一個政黨重用。
這正是鍵必須是（代號, 名稱）的理由——兩種解讀下都安全。
只用代號會把重用的誤併，只用名稱會把改名的誤拆。

⚠️ 代號 1（國民黨）與 16（民進黨）五屆穩定，**那不是代號穩定的證據**。
站台的分桶早就用（代號, 名稱），這條是同一個紀律在新資料集上再次成立。

### 4d. 極限法的界限只約束「混入」，不約束「地理偏誤」

`indigenous-party-preference-bounds.csv` 的區間是 Duncan-Davis 極限法，
在**算術上必然成立**，不依賴任何統計假設。但它約束的只有
「非原住民混入這一層」——**完全不約束樣本代表性**。

⚠️ 三個門檻只涵蓋 11.0%／20.7%／28.4% 的原住民選舉人，
而且全在原住民族地區的山地鄉。**都市裡的平地原住民（約三成）在門檻外**，
極限法對他們給不出有用的界限（區間寬到 0–100%）。

引用時必須說「原住民族地區的政黨票結果」，**不可以**說「原住民的政黨傾向」。
一個無懈可擊的區間被讀成全體的區間，比沒有區間更糟。

⚠️ 守門員本身也有過一個洞：只驗「界限含住觀察值」**擋不住把界限放鬆**——
把 `(1-q)` 寫成 `(1+q)` 時下界被截斷成 0，含住關係照樣成立。
補了寬度恆等式（未截斷時恰為 `(1-q)/q`）才擋得住。
且極限法全程用 `Fraction` 不用 `Decimal`——後者的捨入會讓恆等式差 1 ulp
而逼人設容差，**而容差會把真的放鬆一起放過**。

### 5. 驗證 4／7 檢查的是**來源**，權威值另立 4c／4d

不要為了讓 2005 通過而把第 4 項改用權威值——那會讓它再也偵測不到 `elcand` 損壞。
兩次外部覆核都明確反對那個選項。

---

## 四、事實更正（先前文件寫錯過的）

1. **1994–2006 是 21 個 leaf 資料夾、清點 17 個**（13 原住民檔＋4 同屆區域對照檔）。
   先前記載的「19 個檔」與任何算法都對不上。
2. **2005 屏東縣第十六選區不是「同額競選未投票」。** `elprof` 寫投票數 1,927，
   `elctks` 也有該候選人的列（在鄉鎮市區層級、得票 1,794、註記 `*`），
   只是 `elcand` 的鄉鎮市區欄是 `000` 所以「代碼整組相等」的鍵對不上。
   它就是註記損壞的其中一筆。**因此不需要「elctks 無列時由 elprof 補齊」的回退**，
   而且不該有——實測沒有任何候選人缺 elctks 列，該回退會在 elctks 整區遺失時
   把整區靜默標成當選。
3. **README 的列數表在 2026-08-20 之前就已經錯了**（寫 204,617/5,754/1,332,591，
   四屆版本實際是 266,336/7,335/1,746,316），至少落後兩輪。已更正為九屆數字。
4. **本 repo 不計算「人口比例 vs 席次比例」。** `docs/index.html` 裡「人口」出現 0 次。
   那是隔壁專案 `indigenous-constitution-tw` 的核心指標。
5. **席次跳點的成因是職位不同，不是同一職位斷裂。** 2009-2010 的 63 → 2014 的 190
   來自 D2／R3／R2 在 2014 才納入。現有站台每個圖表都已按選舉種類分開呈現。

---

## 五、工作紀律（違反過才寫進來的）

- **不要 `git add -A`。** 使用者與 AI 共用同一個工作目錄並會並行提交，一律明確列路徑。
  目前 `.gitignore` 有使用者的未提交改動，不要碰。
- **不覆寫來源數字。** 既有處置模式是「原樣保留＋具名記錄＋補償性檢查」。
  外部覆核曾建議寫硬編碼修補直接覆寫，**未採納**。
- **具名到最細的粒度。** 整檔放行等於在該檔停掉那項驗證。
  當選註記具名到候選人、鄉鎮市區錯置具名到選舉區、選舉區欄不一致連
  **允許值**都逐檔釘死。
- **不下未清點的全稱宣稱。** 要寫「不存在」「全部都是」之前先跑一次計數。
- **spec 只寫「終端看到什麼」，不寫「資料流怎麼切分」。**
  「值在哪一層被替換」「哪個欄位保留原樣」是**架構決定（How）**，不是需求（What），
  不可寫成 SHALL。

  ⚠️ 實際事故：`age-99-is-unrecorded` 把
  `Sentinel Values Are Not Presented As Measurements` 寫成
  「值 SHALL 在長表中原樣保留；替換**只發生在呈現端**」。前半句是合格的需求，
  後半句是架構模式。下一個變更 `candidate-age-valid-column` 認定那個分層不夠
  （直接讀長表的人根本到不了呈現端），於是主 spec 一度自相矛盾——
  兩條互相牴觸的 SHALL，都是同一個人在相鄰兩個變更裡寫的。
  最後靠 `## MODIFIED Requirements` 就地改寫才收拾掉。

  **判準**：把句子的主語換成「使用者／消費者」還講得通，才是需求；
  主語只能是「本專案的程式」，那就是架構決定。後者要寫，用註解或 design 決策，
  不要提升到 Requirement 層級去跟自己未來的重構打架。
- **對調欄位語意後，先清點「沿用舊名但語意已翻轉」的讀取點。** 危險的不是改名的地方——
  那些在 diff 裡看得見；危險的是一個字都沒改、但它讀的欄位語意變了的地方。
  做法：`git show HEAD:<檔> | grep -n '\["<舊名>"\]'` 對現況同樣的 grep，逐一判定
  「它要的是舊語意還是新語意」，並講得出「共 N 處、改了幾處、其餘為何不必改」。

  ⚠️ 實際事故：`elected-column-swap` 對調 `當選` 後，驗證第 4 項的 `n_win` 漏改。
  它讀 `當選`（已成權威值）後恆等於比較對象，那項檢查**永不觸發**、
  且因為不觸發所以不報錯，同時汙染了報告的 `當選人數` 欄（2005 兩檔的
  20-vs-27、18-vs-30 差異消失）。全套 pytest 38 passed 完全沒察覺，
  是外部覆核（agy）指出來的。清點結果：建置端舊讀取點 3 處（漏 1）、站台端 0 處。

- **對調欄名時，記得已歸檔的 spec 可能因此變成假的。** 歸檔不代表凍結——
  新變更若推翻舊決定，delta 要用 `## MODIFIED Requirements` 改寫，
  只做 `## ADDED` 會讓主 spec 同時存在兩條矛盾的要求，而且沒有任何檢查會擋下來。

  ⚠️ **但寫了 `## MODIFIED` 不代表它被套用了。** 2026-08-22 實測：
  `spectra archive` 回報「added: 4, **modified: 0, renamed: 0**」卻仍顯示成功，
  MODIFIED 與 RENAMED 都被靜默丟掉。原因是它以需求**名稱**去主 spec 比對，
  而該筆 delta 的 MODIFIED 用的是**改名後的新名**，找不到就跳過。
  `spectra analyze` 有警告「MODIFIED requirement not found in main spec」——
  那句話字面上就是原因，不要像我一樣判斷成「工具不懂 RENAMED 的套用順序」。

  **處置**：MODIFIED 的標題必須用**主 spec 現有的名稱**。要改名就分兩個
  change 做，不要在同一份 delta 裡同時改名與改內容。
  封存後**一定要打開主 spec 確認那條需求真的變了**——只看 archive 的
  「✓ Archived」與 spec 套用行數不夠，`modified: 0` 就藏在那一行裡。
  此版 CLI 沒有 `unarchive` 也沒有 `sync`，補救只能手動套 delta。

  ⚠️ 補救時**不要用 `git checkout <spec>` 還原**——那會把封存已成功套用的
  `ADDED` 一起還原掉，等於自己製造第二個問題（我做過）。
- **驗證要能失敗。** 每加一條驗證，配一個變異測試：改壞必須中止、還原必須通過。
  兩支腳本**已入版控**：`scripts/mutate_build_local_election.py`（**66** 個變異，
  全數被偵測）與 `scripts/mutate_build_site_data.py`（**28** 個——27 個 Python 端
  ＋ 1 個前端 `index.html`，全數被偵測）。兩者都含基準對照（未變異的副本必須通過、
  且不得有測試被跳過）與變異字串唯一性自我檢查。

  ⚠️ 曾有 10 項漏網，全是「把某條驗證拿掉」型，共同點是**那些驗證在乾淨資料上
  永遠不觸發**，拿掉了輸出也不變。插樁量測顯示它們全部執行過、只是條件從未成立
  ——不是死碼。2026-08-22 已為每一項各寫一組會觸發它的合成輸入（見
  `test_unguarded_source_checks`），一項都沒刪。**在補上之前不宣稱受測**，
  那句話當時寫在這裡，現在補完了。

  兩支都在副本上變異，不動真正的
  `scripts/`；唯一的例外是 `docs/index.html` 那一項（測試用 node 讀真檔），
  它會先要求該檔在 git 下乾淨，還原用 `git checkout --` 而非記憶體裡的備份。

  ⚠️ **副本必須放在 repo 根目錄正下方一層（`_mut/`），與 `scripts/` 同深度。**
  測試檔以 `__file__.parent.parent` 推導 ROOT。原本放在 `scratch/mut/`（兩層），
  ROOT 解析成 `scratch/`，於是 `data/processed` 與原始壓縮檔都找不到，
  **迴歸／舊屆／自訂種類三項測試全部靜默跳過、pytest 仍報 passed**。
  那段期間「48 個變異全部被偵測到」完全是靠單元測試達成的。

  ⚠️ **變異腳本的檔名不可用 `test_*.py` 或 `*_test.py`，執行部分要包進 `main()`。**
  原名 `mutation_test.py` 搬進 `scripts/` 後，pytest 依預設樣式收集它，
  **在收集階段就把整套變異跑完**（實測 52 秒）才以 INTERNALERROR 收場。

  ⚠️ 有**十次**是變異測試先漏網、才發現測試無效——不要跳過這一步。
  - 投票率的捨入測試只挑了 52.625，而 `ROUND_CEILING` 在那個值上與
    `ROUND_HALF_UP` 同解，把捨入模式改壞不會被抓到。
    改用 33.333…／66.666… 才同時卡住一律進位與一律捨去。
  - **第十次是同一個結構問題的第三次重演**（`elected-column-swap` 的異常紀錄）：
    我把斷言寫成讀 `data/processed/validation-report.json` 這個**已建好的成品**。
    變異改的是原始碼、不會重建成品，測試對它完全無感。
    正解是掛到 `test_legacy_terms`——它在真實來源上實跑 `process_one` 與
    `cross_validate`，並釘死 `cross_validate` **自己產出的**報告項目。
    判斷法：問「這個變異會改變什麼？」若答案是「某個函式的回傳值」，
    測試就必須呼叫那個函式，不能讀它上次寫出來的檔案。
  - **同一個變異逼了三次才寫對測試**（`年齡_有效 直接抄 年齡`）：
    ①只測 `valid_age()` 函式——變異改的是**列組裝處**，函式沒被動，測試照樣通過。
    **測了規則，沒測規則有沒有被套用。**
    ②改讀**已建好的長表**——變異改的是原始碼、不會重建長表，
    讀成品的測試對原始碼變異一律無感。
    ③實際跑 `process_one` 於合成來源，走過列組裝，才抓得到。
    那 10 項既有漏網與 ①② 是同一個結構問題。
  ⚠️ 站台那支變異測試**在 pytest 下執行**而非直接執行，
  因為這個專案正是在那個落差上出過事（見上面 `@reports`）。
- **註解不可宣稱程式沒做的事。** 外部覆核抓到 `KNOWN_ELECTOR_ANOMALIES` 的
  「上層級必須正常」只寫在註解裡而沒有實作。
- **外部覆核（`codex` MCP／`agy` CLI）送出前，先逐條查證提問裡的事實宣稱。**
  它們抓到的方向常對，細節必須自己驗——這一輪 agy 就有一項宣稱經實測不成立
  （「全面 strip 會讓逐位元組驗收失敗」，實際三張表雜湊相同）。
- **改 `.py` 用編輯工具，不要用 heredoc 跑 Python 改檔**——替換字串裡的 `\n`
  會變成真正的換行，寫出語法錯誤的檔案。**累計踩了十次以上**。
  踩過的形式：純字串、f-string、以及「以行號整行覆寫」（`lines[n] = '...\n...'`）。
  最後一次是在 `candidate-age-valid-column` 改測試檔時，
  寫壞的那一行是 `print("` 後面直接斷行。
  **這條沒有例外**——包括「這次很短應該沒問題」。
---

## 六、量測記錄

### 那 10 項變異漏網（2026-08-22 已補上合成髒資料）

⚠️ **原本寫在這裡的判準是錯的，已作廢。** 舊判準是二分法——
「會改變資料值的邏輯補測試／上游已保證的防禦性檢查當死碼刪掉」。
實際插樁量測後推翻：**九個判斷式全部執行過，只是條件從未成立**。

| 變異 | 到達次數 | 成立次數 |
| --- | ---: | ---: |
| 30 投票數>選舉人數的具名檢查 | 1 | 0 |
| 31 鄉鎮市區配錯選舉區的多重集合 | 3 | 0 |
| 33 寶山鄉的上層級補償 | 3 | 0 |
| 34 鄉鎮市區代碼集合差異 | 9 | 0 |
| 40 選舉區欄的允許值 | 20 | 0 |
| 41 孤兒層級的向上加總 | 4 | 0 |
| 42 孤兒單位的父單位不存在 | 4 | 0 |
| 44 忽略選舉區欄時的身分唯一 | 5 | 0 |
| 45 elctks 比 elprof 細的層級 | 2 | 0 |

**沒有一項是死碼。** 它們不是「上游保證不可能發生」，是「這批資料剛好
沒發生」——刪掉任何一個，下次來源變壞時就沒人擋。

**正確的處置：九項全部補合成髒資料，一項都不刪。** 技術已在
`scripts/test_build_legislative_election.py` 的 `test_synthetic_dirty_data`
驗證過（那支從 20 項漏網修到 0）。工作量在於**每一項的合成輸入都不同**：

- 30 未具名的單位投票數 > 選舉人數｜33 具名單位的上層級也異常
- 31 配錯選舉區的鄉鎮市區多重集合不同｜34 鄉鎮市區代碼多一個或少一個
- 40 選舉區欄出現宣告外的第三種值｜41 孤兒層級加總 ≠ 父層級有效票
- 42 孤兒單位的父單位在 elprof 不存在｜44 忽略選舉區欄後兩人身分相同
- 45 孤兒層級不比 elprof 更深

**第 43 項要另外處理**：它不是「拿掉檢查」而是**讓檢查變空轉**——
移除鍵的選舉區正規化後，四個直轄市檔每一列都變唯一，唯一性檢查恆為真。
要測它得斷言「正規化真的有發生」（例如比對正規化前後的鍵集合大小）。

**處置結果**：十項全部補上合成髒資料，寫在
`scripts/test_build_local_election.py` 的 `test_unguarded_source_checks`，
一項都沒刪。變異測試由 49/59 提升到 **59/59 全部被偵測**，基準對照通過、
無測試被跳過（`scripts/mutate_build_local_election.py`）。
既有四份輸出的 SHA-256 未變。

上表保留作為「沒有一項是死碼」的證據——每一項都執行過，只是條件從未成立。
