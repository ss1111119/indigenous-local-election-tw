# indigenous-local-election-tw

臺灣原住民族選舉資料的整理專案。

⚠️ **repo 名稱裡的 `local` 已經不足以描述涵蓋範圍**（見「尚未解決」第 9 點，
已決定不改名）。實際涵蓋三個資料集，它們是**三個不同的母體，不可互相比較也不可相加**：

| 資料集 | 屆別 | 母體 |
| --- | --- | --- |
| 地方公職（議員、區長、代表） | 1994–2022 九屆 | 各縣市選舉區 |
| 原住民立法委員 | 1995–2024 九屆 | 山地／平地兩個全國選舉區 |
| 不分區政黨票的界限估計 | 2008–2024 五屆 | 原住民族地區的高密度投開票所 |

📊 **線上瀏覽**：<https://ss1111119.github.io/indigenous-local-election-tw/>
（[名錄](https://ss1111119.github.io/indigenous-local-election-tw/roster.html)、[原住民立委與政黨傾向](https://ss1111119.github.io/indigenous-local-election-tw/legislative.html)）

🌐 **English**:
[local elections](https://ss1111119.github.io/indigenous-local-election-tw/en/index.html) ·
[legislators & party leaning](https://ss1111119.github.io/indigenous-local-election-tw/en/legislative.html)
（專案自譯、未經母語者覆核，以中文版為準）

⚠️ **名錄沒有英文版，而且不打算做。** 那一頁有 4,607 位候選人姓名與 27 個選舉區名稱，
那是**音譯**工程不是翻譯：臺灣的人名羅馬拼音有威妥瑪／漢語／通用數套並存，
原住民候選人另有族語名與漢名的問題。本專案沒有權威來源可以決定每個人的名字怎麼拼，
而猜出來的拼法會被當成那個人的英文名。

## 這是什麼

中選會選舉資料庫裡，地方公職人員選舉有十種選舉種類，其中**四種是原住民專屬**：

| 代碼 | 選舉種類 |
| --- | --- |
| T2 | 議員（平地原住民）選舉 |
| T3 | 議員（山地原住民）選舉 |
| D2 | 直轄市原住民區長選舉 |
| R3 | 直轄市原住民區民代表選舉 |

另有 R2 鄉（鎮、市）民代表（平原）選舉，以及原住民族地區的鄉鎮市長與代表。
對照組是 T1 議員（區域）選舉。

本專案處理這些資料：解析、驗證、整理成可用的形式。

## 與其他專案的分工

同一位作者另有兩個相鄰專案，本專案**不重複它們**：

- `indigenous-constitution-tw` — 憲政代表性與資料盤點。已有**立法委員**選舉的
  代表性計算（第 7–11 屆）與 `scripts/extract-cec-elprof.ps1`。
  本專案做的是**地方**選舉，方法論相通但資料結構不同。
- `indigenous-open-data-tw` — 原住民族公開統計的編目與長表（catalog 683 筆）。
  目前不含選舉資料。

## 資料來源

中選會選舉資料庫 `votedata.zip`（約 110MB），涵蓋 1994–2024。
本專案使用其中的地方公職人員選舉：1994、1998、2002、2005、2006、
2009-2010、2014（103 年）、2018（107 年）、2022（111 年）九屆。

壓縮檔內除選舉結果外，另含公民投票與 2022 修憲複決（`rfarea`/`rfprof`/`rftckt`），
以及兩份官方文件：`選舉資料庫格式.odt`（停在民國 101 年，未涵蓋 2024 起的欄位變動）
與 `2022.../選舉種類代碼.xlsx`。公投部分本專案未使用。

⚠️ **該檔案不入庫**——可從中選會重新下載，依「能不能重新取得」的判準不納入版本庫。
取得後請自行驗證雜湊。

### 來源鏈（請自行判斷是否足夠）

本專案**未自行向中選會下載**這份檔案，而是從相鄰專案 `indigenous-constitution-tw`
複製同一份壓縮檔，複製後以 SHA-256 與該專案登記值比對通過
（`84740535b1f4a9a8fec8ebfe8f7577889837b7639cd7f5e40ef8826c6ab2f69a`）。

這證明的是「與相鄰專案那份是同一個檔」，**不是**「與中選會現在提供的那份是同一個檔」。
支持其為中選會產物的是內部證據——壓縮檔內含中選會自己的格式文件與選舉種類代碼表——
而非重新下載比對。若你的用途需要更強的保證，請自 <https://data.cec.gov.tw/> 重新下載並比對雜湊。

## 已查證的事實

以下每一項都實際打開檔案確認過，不是憑資料集頁的敘述。

**壓縮檔內附選舉種類對照表**：`voteData/2022-111年地方公職人員選舉/選舉種類代碼.xlsx`。
不要用猜的解讀代碼。

**壓縮檔內附官方格式文件**：`voteData/選舉資料庫格式.odt`（民國 101 年 4 月）。
⚠️ 它已過時（`elprof` 的 idx11–16 順序與 2022 資料不符），但**欄位語意仍是權威**，
應先讀它再解讀欄位。本專案曾因未讀它而誤記 `elprof` 的 idx6／idx7。

**`elcand` 與 `elctks` 的當選註記有四個值**（官方格式文件明定）：

| 值 | 語意 |
| --- | --- |
| `*` | 當選 |
| （空白） | 未當選 |
| `!` | **婦女保障**（當選） |
| `-` | **因婦女保障被排擠未當選** |

只數 `*` 會系統性少算當選人且不會報錯：2022 年 R2 少 2 席、T1/city 少 3 席、T1/prv 少 1 席。
⚠️ **本專案先前誤稱資料中未出現 `-`。** 實際上 2014 年有 12 筆、2018 年有 8 筆、2009–2010 年有 16 筆，共 36 筆。全部為男性，且全部計為未當選。
35 個出現 `-` 的選舉區**全部同時出現 `!`**（配對出現）；另有 6 個選舉區只有 `!` 沒有 `-`。

**村里代碼的首碼可能是英文字母**：官方格式文件說明，跨多個村里的投開票所由中選會
另行自訂村里代碼且首碼為英文（如 `A001`），對應表見各選舉的「投票所跨村里表」
（壓縮檔內有 `2020總統立委/投票所跨村里表.csv`，25 列）。
原住民各選舉種類目前 0 列，但**行政區代碼一律以字串讀取**因此有官方依據。

**檔案結構與立委資料同構**：每個選舉種類下有 `elbase.csv`、`elcand.csv`、
`elctks.csv`、`elpaty.csv`、`elprof.csv`。

⚠️ **`city`／`prv` 的切分只出現在部分選舉種類**：2022 年僅 C1、T1、T2、T3 有此子資料夾；
D2、R3、R2、D1、R1、V1 為扁平單一份。不要假定所有種類都有兩份。

**行政區 5 碼的語意**（跨選舉種類一致）：第 1 欄縣市／直轄市、第 2 欄鄉鎮市（縣轄）、
**第 3 欄選舉區**、**第 4 欄鄉鎮市區**、第 5 欄村里。
例：T2 臺北市松山區為 `63,000,07,010,0000`（選舉區 07）；D2 高雄市茂林區為
`64,000,00,360,0000`（第 3 欄為 `00`，原住民區長不分選舉區）。
各層級彙總列以其下位欄為 `0` 表示——篩選縣市層級用「第 3–5 欄皆為 0」，
篩選鄉鎮市區層級用「第 4 欄非 0 且第 5 欄為 0」。

**跨屆編碼不一致**：2014／2018 用中文資料夾名（`直轄市平原議員`、`縣市山原議員`），
直轄市／縣市寫在資料夾名裡且結構扁平；2022 改用代碼（`T2`、`T3`）加 `city`／`prv` 子資料夾。
同樣的資訊、兩種編碼，解析器必須兩種都處理。

**`elprof.csv` 的欄位語意**（20 欄，首列為前 6 欄皆 0 的彙總列——
⚠️ 那是**該檔案範圍**的合計，不是全國，見下方 `city`／`prv` 一節）：

| 索引 | 語意 |
| --- | --- |
| idx6 / idx7 / idx8 | **有效票 / 無效票 / 投票數** |
| idx9 | 選舉人數 |
| idx10 | 人口數（底層來源見「尚未解決」第 4 點，未查證） |
| idx11–16 | 候選人數與當選人數，多種版面，以「男+女=合計」自我驗證分辨 |
| idx17 / idx18 / idx19 | 選舉人數對人口數 / **投票數對選舉人數（投票率）** / 當選人數對候選人數 |

⚠️ **`idx6`／`idx7` 曾被本專案誤記為「投票數 男／女」**，2026-08-18 對照壓縮檔內的
官方 `選舉資料庫格式.odt` 後更正。

**這個錯誤是本專案自己生出來的，不是繼承來的**（已查證）：相鄰專案的
`extract-cec-elprof.ps1` 只使用 idx8／idx9／idx10 與 idx11–16，**從未讀取 idx6／idx7**，
其立委輸出不受影響；本專案初始的 `data/sources.json` 也只記了 idx8–10。
「男／女／合計」的標籤是在第一版 README（commit `c465c83`）憑空寫上的——
最可能的來源是看到 idx11–16 有「男／女／合計」的結構，就把同一個模式外推到 idx6–8。

這個錯誤**不會被「男+女=合計」的自我驗證攔下**——`63,768 + 3,067 = 66,835` 一樣成立。
決定性反證來自 D2 與 R3：兩者是**同一批選舉人**（投票數皆 23,712），
idx7 卻是 1,384 與 572。同一群人同一天的性別組成不可能不同，但兩張選票的無效票率當然可以。

**兩個教訓：**

1. **算術自洽不等於語意正確。** 自我驗證只能排除排列錯誤，不能確認欄位語意；
   欄位語意必須對照官方格式文件，或用「同一母體的兩份資料」這類外部約束來驗。
2. **不要把一組欄位的模式外推到另一組。** idx11–16 確實是「男／女／合計」，
   但那不構成 idx6–8 也是的理由。官方格式文件當時就在同一個壓縮檔裡，沒被打開過。

**`city` 與 `prv` 的關係（2022 年實測，T2/T3 皆同構）**：兩者是互斥的行政區劃分，不是同一份資料的兩種切法。
`city` 收錄縣／市（T2 為宜蘭、新竹、苗栗、彰化、南投、屏東、臺東、花蓮 8 縣＋基隆、新竹 2 市；T3 為宜蘭、新竹、苗栗、南投、嘉義、屏東、臺東、花蓮 8 縣），
`prv` 收錄六都（臺北、高雄、新北、臺中、臺南、桃園）。判斷依據：`elbase.csv` 中代表各層級彙總列（第 3–5 欄為 `00,000,0000`）的行政區代碼，`city` 只出現代碼 `10,0xx`（縣市），`prv` 只出現 `63`–`68`（六都），兩者無重疊。

⚠️ 兩份 `elprof.csv` 首列（前 6 欄皆 0）**各自只是該檔案範圍內的小計，不是全國合計**——`city` 首列是「縣市小計」、`prv` 首列是「六都小計」，需相加才是全國數字。

2022 年實測：

| | 選舉人數 | 投票數 | 投票率 | 候選人 | 當選 |
| --- | ---: | ---: | ---: | ---: | ---: |
| T2 平地原住民議員 — city（縣市） | 112,461 | 66,835 | 59.43% | 46 | 23 |
| T2 平地原住民議員 — prv（六都） | 90,016 | 39,738 | 44.15% | 27 | 11 |
| T2 平地原住民議員 — 全國合計 | 202,477 | 106,573 | 52.63%* | 73 | 34 |
| T3 山地原住民議員 — city（縣市） | 140,990 | 102,752 | 72.88% | 50 | 24 |
| T3 山地原住民議員 — prv（六都） | 75,272 | 41,412 | 55.02% | 30 | 11 |
| T3 山地原住民議員 — 全國合計 | 216,262 | 144,164 | 66.66%* | 80 | 35 |

投票率經交叉驗證：66,835 / 112,461 = 59.43%、39,738 / 90,016 = 44.15%，皆與檔案 idx18 印出的值一致。
＊全國合計投票率為本專案計算值（合計投票數 / 合計選舉人數），非檔案內任何一列直接印出的數字。

**D2 與 R3 是同一批選舉人的兩種選舉**（2022 實測）：兩者皆只在四個直轄市舉行，
涵蓋六個原住民區——高雄市茂林、桃源、那瑪夏；新北市烏來；臺中市和平；桃園市復興
（自 `D2/elbase.csv` 逐列讀出，非引用外部名單）。
選舉人數分別為 31,807（D2）與 31,803（R3），差 4 人。
同一群選民、同一天、兩種職位，是少見的乾淨對照。

## 尚未解決

1. ~~**更早屆別（1994–2010）尚未納入**~~ **已納入（2026-08-20）**。
   九屆全部進入建置，處置見「更早的屆別」。
   鄉鎮市區層級的跨檔正規化已於 2026-08-22 完成——那六個檔的 1,290 個
   鄉鎮市區單位皆換算為同屆「區域」檔口徑。
2. **地方層級的代表性指標尚未定義**。各縣市席次、選區與選舉人範圍不同，
   全國加總後除以席次會掩蓋地方差異，不能直接套用立委那套算法。
   ⚠️ 站台已於 2026-08-23 加入立委頁，但那是**另一個資料集的呈現**，
   不是這個指標的答案——立委的全國選舉區算得出來的東西，地方層級算不出來。
   ⚠️ **依 `election-period-publication`，選舉期間（現在）不得定義這個指標**——
   它兩問皆是（需跨母體相除、帶方向），屬解讀性指標。2026-08-24 與 Codex、
   Antigravity 討論過方向，結論收窄為：選舉期間只列**哪些分母被排除及理由**
   （縣市席次分配邏輯不同、`人口數` 底層來源未查證見第 4 點、選區範圍不同），
   **不選定分母、不算任何比值**——分母的選擇本身就是一個帶方向的判斷，
   留到 2026-12-04 公告當選人名單後再做。列出排除清單本身不算新增解讀性指標。
3. **納入哪些選舉種類未定**。T2/T3 議員、D2 區長、R3 區民代表、R2 鄉鎮市民代表、
   原住民族地區的鄉鎮市長／代表，不應先假定都用同一指標。
4. **`elprof` 的 `人口數` 欄底層來源未查證**。一般認為中選會此欄係彙編內政部戶政司
   戶籍資料後發布、非中選會原生統計，但本專案**尚未查到明文依據**，故不作此宣稱。
   若成立，引用時「中選會發布的數字」與「中選會產生的數字」標法不同。
   相鄰專案在立委資料上曾與戶政司年底人口交叉驗證，各屆差 4 千至 3 萬（基準日時間差），
   該結果與本欄的來源歸屬是兩件事，不可互相推論。
   ⚠️ **待辦（非急迫）**：`docs/index.html` 附錄的「全國選舉人數 ÷ 席次」比值已標示
   「這不是代表性指標」，但沒有在同一處直接提示分母（人口數）的來源未查證——
   讀者要跳到本文件才查得到。是否要把這段警告搬一份過去，2026-08-24 尚未決定。
5. **本專案未自行向中選會下載原始壓縮檔**，見「資料來源／來源鏈」。
6. ~~**更早屆別是否可用未確認**~~ **已完成清點（2026-08-19）**。1994–2006 的地方民代
   共 **21** 個 leaf 資料夾，已逐檔開啟檢視其中 **17** 個（13 個原住民檔＋4 個同屆
   「區域」對照檔，扣除 4 個直轄市議員的區域腿）。結論見「更早的屆別」：縣市山原／平原
   1998 起連續可用，直轄市為合併類別、1994 為兩種已廢除或無明細的職位，
   必修陷阱已具名記錄。
   ⚠️ 本項先前記載「19 個檔」，該數字與任何一種算法都對不上，已於重新清點後更正。
7. ~~**2005 縣市議員的當選人數處置尚未實作**~~ **已實作（2026-08-20）**。
   `elcand` 的當選註記原樣保留於 `當選註記`，`當選` 改存由 `elctks` 推導的權威值，
   不符的 63 位候選人（2005 兩檔 61＋1994 高雄市 2）逐一具名記錄。
   ⚠️ 先前記載「唯一不符的屏東縣第十六選區是同額競選未投票」——**該敘述不成立**：
   該區 `elprof` 寫投票數 1,927、`elctks` 也有該候選人的列（只是在鄉鎮市區層級），
   它就是註記損壞的其中一筆。
8. ~~**`縣市`／`鄉鎮市區` 是否比照 `當選`／`年齡` 對調成正規化值**~~
   **已決定：不對調（2026-08-23）**。維持「原值在本名、正規化值在 `_正規化`」。
   `當選` 與 `年齡` 之所以值得對調，是因為**天真的用法會得到錯的答案**——
   直接 `AVG(年齡)` 會把 `99` 這個無資料哨兵算進去，直接數來源的當選註記
   在 2005 兩檔會少 19 席。`縣市`／`鄉鎮市區` 不是這種情況：來源碼在**同一屆檔內
   是正確的識別碼**，天真的用法（單屆內分組）本來就對；只有跨屆比對需要正規化值，
   而那是刻意的動作，會去找那個具名欄位。
   另外兩個理由：`鄉鎮市區_正規化` 只存在於地方公職、立委那邊沒有，對調會讓兩個
   資料集的欄位語意更加分歧；而換欄位語意的代價本專案付過——`當選` 那次的漏網
   處在 diff 裡一個字都沒改、只是讀的欄位語意翻了，38 項測試全綠。

9. **repo 名稱**：`indigenous-local-election-tw` 的 `local` 已不足以描述涵蓋範圍。
   **已決定：不改名（2026-08-23）**。名稱不準確的唯一實害是讀者誤判涵蓋範圍，
   而那已由 repo description 與本文開頭的資料集表修正，代價為零。
   改名的代價則是 **GitHub Pages 網址失效**——git remote 有永久轉址，**Pages 沒有**，
   而已經流出去的連結無法回收。若日後決定要改，repo 內需同步的位置是
   `README.md`（6 處）、`docs/sitemap.xml`（3 處）、`docs/三屆概況.md`（1 處）；
   相鄰三個 repo 實測**零處引用**。
   ⚠️ **本機資料夾不可跟著改名**：工具的記憶目錄以本機路徑為鍵。

## 發布上的限制

本專案做的是**歷史選舉資料的整理與呈現**，不做預測、不做選情追蹤、不對 2026 年
這屆選舉作任何評價。基於這個定位，只有兩條限制：

1. **`elcand` 的出生日期、出生地、學歷不輸出。**
   中選會〈網站資料開放宣言〉明訂：個人資料能否蒐集處理利用，使用者須自行依
   個人資料保護法辦理——開放授權不免除這一條。這是法律要求，不是本專案的價值判斷。
2. **不產出 2026 這屆的預測或選情指標。**
   這條管的是**解讀**（不當選戰參與者），與資料本身無關。歷史原始數據照原解析度發布。
3. **發布依選舉期程分階段，且已經在選舉期間內。**
   2026 年地方公職人員選舉的選舉公告已於 **2026-08-20** 發布、投票日 **2026-11-28**、
   公告當選人名單 2026-12-04 前（中選會第 622 次委員會議通過之工作進行程序表）。

   | 階段 | 准許發布 |
   | --- | --- |
   | 選前（至 2026-08-19） | 方法、資料字典、已凍結的歷史數據與固定的計算規則 |
   | **選舉期間（2026-08-20 至 12-04，現在）** | 不新增解讀性指標；保留的歷史資料須明文標示不代表本屆 |
   | 結果確認後（2026-12-04 起） | 本屆官方彙總，附快照、口徑、缺漏與計算式 |

   ⚠️ **「選前只做歷史比較安全」的推論不成立**——歷史落差在選戰期間同樣會被
   當成攻防工具。這是 2026-08-18 兩輪外部覆核的共識，不是本專案的臆測。

   逐頁的判定、判定理由與被凍結的指標在 [`docs/發布判定紀錄.md`](docs/發布判定紀錄.md)。
   `docs/` 下每一頁都必須在那份紀錄裡，由 `build_site_data.py` 兩個方向機器驗證。

### 被撤銷的限制，以及為什麼

早期版本曾規劃「輸出聚合到鄉鎮市區層級以上、不散布投開票所級資料」，
理由援引 CARE 原則與原住民族資料主權。**該限制已於 2026-08-18 撤銷**，理由：

- **範疇錯誤**：CARE 與資料主權的核心是文化、土地、基因、傳統知識這類被掠奪的資料；
  選舉結果是民主問責的公共產物，投票無記名，投開票所資料呈現的是集體政治傾向。
- **保護不了任何東西**：資料已由中選會全網公開且授權可再散布、不可撤回。
- **家父長主義**：本專案不會對 T1 區域議員的同類資料設此限制，只對原住民選舉種類設，
  等於宣稱原住民的選舉資料需要特殊照管。實際擋到的是缺技術資源的獨立記者與部落青年，
  而非早已有能力解壓縮原始檔的政黨。
- **在自己的前提下就失效**：茂林區本身只有 3 個里，聚合到區級一樣高度可辨識。

⚠️ **這段推論的過程本身有一個教訓**：撤銷前的兩輪外部覆核中，
**提問者以誘導性問句引導了兩個外部模型**（「這是不是正好落入你們自己駁回過的陷阱」），
兩邊因此都產出了嚴格的限制條文，其中一個模型還虛構了本專案並不具備的資料處理能力。
第三輪坦白誘導後重新提問，兩邊都同意應撤銷。
**外部覆核的價值取決於提問是否中立**——這件事已記入 `docs/規劃-2026地方選舉.md`。

完整推導見 [`docs/規劃-2026地方選舉.md`](docs/規劃-2026地方選舉.md)。

## 已產出

`python scripts/build_local_election.py` 由 `data/raw/cec-votedata.zip` 建立
（不連網；任何自我驗證未通過即中止，不產出半套結果）：

| 檔 | 列數 | 內容 |
| --- | ---: | --- |
| `data/processed/cec-local-election-summary-long.csv.gz` | 267,876 | 各行政層級的有效票／無效票／投票數／選舉人數／人口數／候選當選人數／投票率 |
| `data/processed/cec-local-election-candidates-long.csv` | 7,818 | 候選人：政黨、性別、年齡、現任、當選註記與**當選權威值**（28 欄，**不含出生日期、出生地、學歷**）。⚠️ `年齡` 是**乾淨值**（未記載留空），直接 `AVG(年齡)` 即正確；來源原值含無資料值 `99` 者在 `年齡_原始` |
| `data/processed/cec-local-election-votes-long.csv.gz` | 1,751,190 | 候選人得票，最細到投開票所 |
| `data/processed/validation-report.json` | — | 驗證結果與來源檔 SHA-256 |
| `data/processed/cec-moi-crosswalk.csv` | 8,344 | 中選會↔內政部行政區代碼對照（`python scripts/build_crosswalk.py`）|
| `data/reference/cec-county-code-crosswalk-1998-2002.csv` | 31 | 1998／2002 縣市代碼→同屆「區域」檔代碼。**這是輸入不是產出**，故不放在 `data/processed/`——清空輸出目錄不會把它刪掉 |
| `data/reference/cec-town-code-crosswalk-1998-2005.csv` | 1,290 | 1998／2002／2005 原住民分項檔的鄉鎮市區代碼→同屆「區域」檔代碼（829 列兩者不同）。由 `python scripts/build_town_crosswalk.py` 產生，但建置每次都由來源重新推導再逐列比對，脫節即中止 |

**目前範圍：1994、1998、2002、2005、2006、2009-2010、2014（103 年）、
2018（107 年）、2022（111 年）九屆。**

### 另一組：原住民立法委員（九屆，2026-08-21 新增）

| 檔 | 列數 | 內容 |
| --- | ---: | --- |
| `data/processed/cec-legislative-election-summary-long.csv.gz` | 197,270 | 選舉概況，逐行政層級 |
| `data/processed/cec-legislative-election-candidates-long.csv` | 163 | 候選人（**不含出生日期、出生地、學歷**）|
| `data/processed/cec-legislative-election-votes-long.csv.gz` | 1,697,134 | 候選人得票 |
| `data/processed/legislative-validation-report.json` | — | 驗證結果與來源檔 SHA-256 |
| `data/reference/cec-legislative-county-crosswalk.csv` | 69 | 三套縣市代碼系統的對照（建置的**輸入**，非產出）|
| `data/processed/cec-party-list-summary-long.csv.gz` | 120,661 | 不分區政黨票的選舉概況，投開票所層級，含每所的原住民選民佔比 `p` 與投票者佔比 `q` |
| `data/processed/cec-party-list-votes-long.csv.gz` | 1,848,890 | 逐所逐政黨得票 |
| `data/processed/cec-party-list-seats.csv` | 98 | 逐屆逐政黨的兩階段得票率與席次（來源原值，不重算）|
| `data/processed/indigenous-party-preference-bounds.csv` | 228 | ⚠️ **估計值，不是開票結果**——原住民政黨傾向的觀察值與數學界限。與官方數字**分表**，每個數值欄帶 `觀察_`／`下界_`／`上界_` 前綴 |

涵蓋 1995、1998、2001、2004、2008、2012、2016、2020、2024 九屆的
山地原住民（`L3`）與平地原住民（`L2`）立委，合計 60 席。
建置：`python scripts/build_legislative_election.py`。
完整說明見 [`docs/schema/cec-legislative-election.md`](docs/schema/cec-legislative-election.md)。

⚠️ **這是與地方公職完全分開的資料集**，檔名前綴與欄位都不同。
`L2`／`L3` 是**本專案自訂代碼**，不是官方代碼；刻意與議員的 `T2`／`T3` 區隔——
兩者都叫「平地／山地原住民」，但一個是議員、一個是立委。

⚠️ **席次逐屆不同**：1995 各 3 席、1998／2001／2004 各 4 席、2008 起各 3 席。
不可寫成單一數字。

⚠️ **2016 的最細層級只到鄉鎮市區**（其餘八屆為投開票所或鄉鎮市區依屆別而定）。
該屆來源的村里以下不完整——1,402 個單位在 `elctks` 沒有對應列、50 個單位
有效票不符——故不輸出，而不是輸出後在文件加註。

⚠️ **縣市代碼有三套系統**，且 2012 那屆與其他兩期都不同（宜蘭縣在 1995 與 2020
都是 `002`、在 2012 是 `001`，而 `001` 在 1995 是**臺北縣**）。跨屆比對請用
`縣市_正規化`，或先以 `admin_code_system` 欄確認同系統。
`鄉鎮市區_正規化` 同理：1998／2002／2005 原住民分項檔的鄉鎮市區碼是檔內重編的，
此欄已換算為同屆「區域」檔口徑（1,290 個單位、829 個換算後與原碼不同）。

⚠️ **不是每一屆都可以放進同一條折線。** 長表有 `is_main_sequence` 欄：
1994 台灣省議員（精省後廢除的職位）與各屆直轄市議員的「原住民」合併類別
（未分平地／山地）標為 `false`，共 39 列。畫跨屆折線前先篩 `true`。

✅ **算席次直接用 `當選`（`Y`／`N`），不必先讀文件。**
它存放的是由 `elctks` **跨檔比對後的權威判定**，不是來源怎麼寫。

**來源的認定完整保留在 `當選註記`（原樣）與 `當選註記語意`（解碼）**——
`當選註記` 為 `*` 或 `!` 即為來源認定的當選。⚠️ 但那**會反映來源的已知錯誤**：
2005 縣市議員兩檔與 1994 高雄市的該欄損壞（以註記計，2005 山原只有 18 席、
平原 20 席，正確為 30 與 27）。九屆 7,818 筆中兩者有 **63 筆**不一致，
全部落在 2005 T2（33）、2005 T3（28）與 1994 直轄市合併類別（2）三處，
其餘 30 組（屆別 × 選舉種類）零不一致。

`當選_依據` 記錄權威值取自 `elctks` 的哪一層（727 筆取自鄉鎮市區層級，依據較弱）。

⚠️ **`elected_authoritative` 欄已移除**（其值即現在的 `當選`），
`elected_authoritative_basis` 更名為 `當選_依據`。
候選人長表的欄位數由 29 變 **28**——**不可用位置索引取值**，
移除欄位會使其後所有欄位左移一格，依位置讀取者不會收到錯誤訊息。

⚠️ **1994–2006 的席次是這樣分布的**：1994 共 6 席（省議員山原 2、平原 2、
直轄市合併 2）；直轄市合併類別 1994／1998／2002／2006 各 2 席；
縣市議員 T2 23→26→27、T3 30→30→30（1998／2002／2005）。

⚠️ **2009-2010 是同一輪任期、分兩次投票**——2014 年九合一整併之前，
縣市議員（2009-12-05）與直轄市議員（2010-11-27）的選舉不同天。
**這是本專案的建模判斷，不是來源直接給的分屆方式。**

⚠️ **該屆只有 T2／T3／T1**：D2／R3 當時不存在（直轄市原住民區依 2014 年
修正的地方制度法設立）；R2 因 2010 年的鄉鎮市民代表為全國合併檔、
`elbase` 中無任何可辨識平地原住民席次的名稱而無法分離。

⚠️ **該屆的行政區代碼是完全不同的編碼**，與 2014 起的不可互通：
2009 宜蘭縣 `03,002`、2010 臺北市 `01,000`，而 2014+ 是 `10,002`／`63,000`
（內政部戶役政代碼）。故該屆**不納入 crosswalk**，跨屆比對行政區時必須排除。

以下表格為 2014／2018／2022 三屆（2009-2010 缺三種選舉種類，並列會誤導）：

投票率（%）：

| 種類 | 2014 | 2018 | 2022 | 14→18 | 18→22 |
| --- | ---: | ---: | ---: | ---: | ---: |
| T2 議員(平地原住民) | 58.96 | 59.96 | 52.63 | +1.00 | **−7.33** |
| T3 議員(山地原住民) | 72.76 | 73.32 | 66.66 | +0.56 | **−6.66** |
| D2 直轄市原住民區長 | 80.93 | 80.45 | 74.55 | −0.48 | −5.90 |
| R3 直轄市原住民區民代表 | 80.94 | 80.45 | 74.56 | −0.49 | −5.89 |
| R2 鄉(鎮、市)民代表(平原) | 63.13 | 65.21 | 60.70 | +2.08 | −4.51 |
| T1 議員(區域)　**對照組** | 67.69 | 67.00 | 61.17 | −0.69 | −5.83 |

席次：T2 33→34→34、T3 33→35→35、D2 6→6→6、R3 50→50→50、
R2 68→71→72、T1 841→843→841。

⚠️ **兩個必須一起看的對照：**

1. **對照組同步下滑**——T1 區域議員 2018→2022 也降 5.83 個百分點。
   沒有對照組，很容易把全國性的變化誤讀成族群特有的現象。
2. **這是單一屆的變化，不是趨勢**——2014→2018 幾乎持平（−0.69 到 +2.08），
   2018→2022 才全面下滑。本專案先前只有兩屆時曾把它描述成「下滑」，
   加入第三屆後修正：**兩個點畫不出趨勢。**

本專案只呈現數字，不對成因作解釋。
有 `city`／`prv` 兩份者，投票率為本專案計算值（相加後重算），原始檔中不存在。

⚠️ **不同選舉種類之間不可相加**——D2 與 R3 已實測為同一批選民
（投票數皆 23,712），相加會重複計算。T1 是對照組，不是原住民資料。

欄位說明與七個必讀陷阱見 [`docs/schema/cec-local-election.md`](docs/schema/cec-local-election.md)。

### 能不能 join 到內政部的圖資與人口統計？能，但要用代碼

實測 2014／2018／2022 三屆：**鄉鎮市區 368/368 完全對上（100%）**、村里 7,688/7,976（96.39%）。
對應規則為 `CEC 省市+縣市+鄉鎮市區 = MOI TOWNCODE`，村里再接「村里碼去掉第 1 碼」。

⚠️ **但不能用名稱 join**：中選會與內政部的名稱都含 Unicode 私用區字元
（Big5 遺留，如 `U+E02D` 即「廍」，多數字型無字形）。本專案輸出中有 8 個 PUA 碼位、
影響 35 個行政區與 2 個候選人姓名。已驗到原始位元組層級——是來源如此，不是解碼錯誤。

對照表與完整說明見 [`docs/schema/cec-moi-crosswalk.md`](docs/schema/cec-moi-crosswalk.md)。

⚠️ **T2／T3 有將近四成的列是「全零列」**（選舉人數、投票數、人口數皆為 0，
三屆合計 47,536 列）。平地／山地原住民選舉人散布在全縣每個投開票所，
多數投開票所沒有這類選舉人。算「平均每投開票所多少選舉人」這類問題前
**必須先篩掉**，否則會低估一半。詳見 schema。

⚠️ **跨屆的編碼完全不同**：2018 的欄位全部加引號、代碼前綴 `'`（Excel 強制文字）、
合計列在**最後一列**而非第一列、`elprof` 版面是「合計在前」而 2022 是「男女合計」。
解析器兩種都處理；版面由「男+女=合計」自我驗證自動分辨，不是寫死的。

建置時執行 74 個中止點的自我驗證（讀檔期、鄉鎮市區正規化、當選權威值、
年齡哨兵、跨檔對帳五階段），最強的三項：

- **逐一行政單位對帳**：九屆共比對 **267,481 個單位**（2022 單屆 71,631），
  每個單位的候選人得票加總必須等於該單位的有效票。
  只比對總和會放過互相抵銷的錯誤。
- **投票率逐列精確重算**：九屆共 **206,959 列**，**不設容差**。中選會用四捨五入
  （`ROUND_HALF_UP`）而非 Python `round()` 的銀行家捨入——實測前者全部
  精確吻合，後者光 2022 一屆就有 47 列差 0.01。慣例確認後，
  容差只會讓真錯誤藏起來。
- **候選人與得票的雙向參照完整性**：每位候選人至少一筆得票列，
  且每個得票鍵都對應到候選人。候選人的行政區粒度隨選舉種類而異，
  由資料推導而非寫死。

⚠️ **測試在 pytest 下曾經無法失敗。** 7 個 `test_*` 函式全都沒有 `assert`，
只把失敗記進全域清單後正常返回——實測故意改壞預期值，`pytest` 仍報
「7 passed / exit 0」，而 docstring 當時還寫著「兩者皆可」。
已加 `@reports` 包裝，現在會正確報 `1 failed`。
**測試數量再多，若失敗不會讓 runner 失敗，數量就沒有意義。**

`python scripts/test_build_local_election.py` 跑 22 組、451 項檢查
（單元＋整合＋迴歸）。**已用變異測試確認測試真的會失敗**，並因此抓到一個驗證本身的
漏洞：`city`／`prv` 互斥檢查原本寫死只比對前兩份，分組寫錯也照樣通過，
已改為組內全配對。**驗證通過不代表驗證有效。**

`python scripts/test_build_site_data.py` 跑 44 組、339 項檢查，
**只放 `--check` 抓不到的東西**——`--check` 的辨識力全部來自「現有資料剛好會
觸發那條分支」，凡是真實資料觸發不到的（防禦性的 raise、零分母、剛好落在
四捨五入半數上的值），改壞了它一樣通過。61 項變異全數被偵測到，
且變異測試**在 pytest 下執行**而非直接執行，因為這個專案正是在那個落差上
出過事。其中一項含「@reports 是否承重」的驗證：同樣壞掉的程式，
有 `@reports` 時 pytest 失敗、拿掉後 pytest 通過。

兩支變異測試腳本**在版控內**，不是一次性的暫存工具：

- `python scripts/mutate_build_site_data.py` —— 61 個變異（52 個 Python 端
  ＋ 9 個真檔／前端），**全數被偵測到**
- `python scripts/mutate_build_local_election.py` —— 66 個變異，
  **全數被偵測到**（2026-08-22 實測，基準對照乾淨：未變異通過且無測試被跳過）

⚠️ 那 10 項曾經全是「把某條驗證拿掉」型的變異，共同點是**那些驗證在乾淨資料上
永遠不觸發**，拿掉了輸出也不變，所以當時的測試看不出差別。
插樁量測顯示它們**全部執行過、只是條件從未成立**——不是死碼。
2026-08-22 已為每一項各寫一組會觸發它的合成輸入，一項都沒刪。
**在補上之前，那些驗證不能算受測**——這是當時寫在這裡的話，現在補完了。
（先前文件寫「48 個變異全部被偵測到」，那是在副本深度錯誤、迴歸測試整批靜默跳過的
情況下得到的數字，不成立。）

兩支都把原始碼複製到 `_mut/` 再改壞副本，不動真正的 `scripts/`。
⚠️ **`_mut/` 必須在 repo 根目錄正下方一層**，與 `scripts/` 同深度——測試檔以
`__file__.parent.parent` 推導專案根目錄去找 `data/` 與 `docs/`。深度錯了不會報錯，
只會讓需要那些檔案的測試**靜默跳過而 pytest 仍報 passed**。
⚠️ 檔名刻意不用 `test_*.py`／`*_test.py`，執行部分包在 `main()` 裡，
否則 pytest 會把它們當測試檔收集、在收集階段跑完整套變異。

## 三屆概況

[`docs/三屆概況.md`](docs/三屆概況.md) 以**三屆**資料呈現規模、投票率、政黨、
性別、現任者與同額競選。**以數字呈現為主，不解釋成因。**
⚠️ 該文件的範圍比資料集小——**九屆完整版在網頁**，見上方連結。
另有兩個單檔 HTML（不連外部資源、淺深色皆可讀、互相以相對路徑連結）：

- [`docs/index.html`](docs/index.html) —— 概況：投票率、政黨、性別、規模，**九屆**。
  該屆沒有的選舉種類**標示為不存在，不以 0 填補**——
  折線圖留空、表格寫「無此選舉」或「—」。
  臺灣省議員與直轄市議員的「原住民」合併類別**不進跨屆折線**，另以獨立區塊呈現。
- [`docs/roster.html`](docs/roster.html) —— **名錄**：逐縣市、逐選舉區列出
  所有候選人與當選人，含政黨、現任與婦女保障標記。支援搜尋與
  網址雜湊（`#2018/D2`）可直接連到特定檢視。

### 圖表配色的可及性驗證

先前這裡只寫「配色以 CVD 驗證器檢過」——沒有工具、沒有模擬類型、沒有門檻，
**那句話無法查核**。以下是可重跑的記錄。

**工具**：`python scripts/palette_metrics.py "<色1,色2,色3,色4>" --labels ... --all-pairs`
（in-repo）。做法為 Machado 2009 severity 1.0 矩陣套用在**線性** RGB
（套在 gamma 空間會差 0.6~1.2）。對照外部驗證器校準：常人差 ≤ 0.04、protan 0.01、deutan 0.03。

**模擬類型**：**protan 與 deutan**。⚠️ **tritan 未實作**，因此本專案不宣稱 tritan
（Viénot 單平面與 Machado 矩陣都與外部驗證器對不上，差 4~18）。

**門檻**：系列**全配對**（不只相鄰）常人 ΔE ≥ 15、protan／deutan ≥ 8（OKLab ×100）；
段內數字對自己的填色 WCAG ≥ 4.5:1。明暗主題**各自**量測，暗色不由亮色反推。

| | 亮色主題 | 暗色主題 |
| --- | --- | --- |
| 全配對最小 ΔE | 常人 16.9／色盲 9.0 | 常人 16.6／色盲 11.1 |
| 無黨籍 ↔ 其他（兩個中性色） | 常人 **39.6**／色盲 **39.5** | 常人 **30.4**／色盲 **30.3** |
| 國民黨 `--s1` | `#2a78d6` 墨色 `#000` 4.76:1 | `#3987e5` 墨色 `#16181A` 4.89:1 |
| 無黨籍 `--s2` | `#3A4046` 墨色 `#fff` 10.49:1 | `#C9CED3` 墨色 `#16181A` 11.23:1 |
| 民進黨 `--s3` | `#1baf7a` 墨色 `#16181A` 6.32:1 | `#1da77a` 墨色 `#16181A` 5.82:1 |
| 其他 `--s4` | `#ADB3B9` 墨色 `#16181A` 8.41:1 | `#6A7178` 墨色 `#fff` 4.95:1 |

由 `scripts/test_site_invariants.py` 的 `test_series_palette_separation` 與
`test_in_mark_label_contrast` 自動守住。

⚠️ **無黨籍不用橘色。** 橘色在臺灣政治語境是親民黨的顏色，而親民黨確實在這份資料裡
（128 位候選人、40 席、跨七屆，歸在「其他」桶）。橘色長條標著「無黨籍」會被誤讀，
所以橘色**釋放且不指派給任何桶**。改用中性色：亮色主題走深、暗色主題走淺——
因為暗色的「其他」已是中灰 `#6A7178`、面板是 `#1D2023`，再往深會撞面板。
⚠️ **紫色不可用**：實測紫與國民黨藍在 protan／deutan 下只差 ΔE 6.2（亮）／5.9（暗），
低於門檻，且那是**非相鄰**配對——這是把門檻從相鄰擴到全配對的直接原因。

⚠️ **已知未達標、尚未處理**：填色對底色的 WCAG 非文字對比，亮色主題有兩項低於 3:1——
民進黨 `#1baf7a` **2.70:1**、其他 `#ADB3B9` **2.03:1**。這是既有配色的問題，
而且目前**沒有任何 spec 要求「填色 vs 底色」的對比**（只規範段內文字對填色）。
改動民進黨的綠會牽動全配對的瓶頸，未在本輪處理。

⚠️ **兩份 HTML 的資料常數由腳本產生，不要手改。**
`python scripts/build_site_data.py --write` 由 `data/processed/` 的 summary 與
candidates 兩張長表算出 `index.html` 的 `DATA` 與 `roster.html` 的 `D`，
以固定標記行界定、**只替換該行**，其餘位元組不變。
`--check` 驗「由現有資料重建的常數 == 檔案現況」，不一致即非零退出。
先前這兩個常數是手動維護的，那正是站台落後資料集四屆的原因。

**線上版**：<https://ss1111119.github.io/indigenous-local-election-tw/>
（名錄在 [`/roster.html`](https://ss1111119.github.io/indigenous-local-election-tw/roster.html)，
支援網址雜湊如 `roster.html#2009-2010/T3`；原住民立委與政黨傾向在
[`/legislative.html`](https://ss1111119.github.io/indigenous-local-election-tw/legislative.html)）

⚠️ **站台涵蓋三個資料集，它們不可互相比較，也不可相加。**

| 頁面 | 母體 | 屆別 |
| --- | --- | --- |
| `index.html`／`roster.html` | 地方公職（議員、區長、代表） | 1994–2022 九屆 |
| `legislative.html` 前三節 | 原住民立法委員（山地／平地兩選舉區） | 1995–2024 九屆 |
| `legislative.html` 第四節 | 原住民族地區高密度投開票所的不分區政黨票 | 2008–2024 五屆 |

三者的選舉區、任期與選舉人母體都不同。第三個資料集另有涵蓋率限制：
最嚴的門檻只涵蓋 11.0% 的原住民選舉人，且全在山地鄉，
頁面上的限定語與數字放在同一個可複製的區塊內。

兩份都放在 `docs/`，因為 GitHub Pages 只支援根目錄或 `/docs`，不支援其他資料夾；
由 `main/docs` 發布，`docs/.nojekyll` 關掉 Jekyll 處理。

該文件經兩輪外部覆核後改寫，主要修正：政黨呈現格式對所有政黨統一
（原本無黨籍給比例、國民黨給席次、民進黨給提名／當選，讀起來有針對性）；
D2 只有 6 席，一律改以「幾席／總席次」呈現而非百分比；補上女性**候選人**
數字（D2 三屆有 3 筆女性候選人紀錄，皆未當選——不補會被讀成沒有女性參選）；
「對照組」改為「參考組」；「無黨籍」改為原始檔的官方名稱「無黨籍及未經政黨推薦」。

## 更早的屆別（已納入，2026-08-20）

原住民地方選舉的資料回溯到 **1994 年**，已全部納入建置。以下為納入前的清點結果
（皆為實測），以及納入時的處置。

### 資料深度差三個量級

| 期間 | `elprof` 最細層級 | 列數（以平地原住民為例）|
| --- | --- | ---: |
| 1994 | **只有合計列** | 1–3 |
| 1998–2006 | 鄉鎮市區 | 201–254 |
| 2009 起 | 投開票所 | 7,175 |
| 2014 起 | 投開票所 | 7,306 |

**1994 幾乎沒有地理明細**——`elbase` 只有「臺灣省」一列，整個檔就是一個總數。
它是一個標題數字，不是資料集。

### ⚠️ 直轄市的原住民議員在 2006 年以前是**合併類別**

1994／1998／2002／2006 的直轄市資料夾就叫「原住民」，涵蓋臺北市與高雄市，
**沒有分平地／山地**。2014 年起才分成「直轄市平原議員」與「直轄市山原議員」。

因此 **1994–2006 的直轄市原住民議員無法對應到 T2 或 T3**——它兩者都不是。
若要納入，必須新增一個選舉種類代碼，或明確標記為「合併類別」。

⚠️ **更正（2026-08-19 清點）：合併只發生在直轄市那一支，不是整個 1994–2006。**
先前把這件事寫成「納入這些屆別的主要障礙」，範圍估過頭了。實測：

| 選舉 | 子資料夾 |
| --- | --- |
| 1998／2002／2005 縣市議員 | 區域／**山原／平原**（已分開） |
| 1994 台灣省議員 | 區域／**山原／平原／平原2**（已分開） |
| 1994／1998 直轄市議員 | 區域／**原住民**（合併） |
| 2002／2006 直轄市議員(原住民) | 單一目錄（合併） |

**山原／平原的分野 1994 就存在**，1998 起在縣市層級連續到 2022，且有選舉區與
鄉鎮市區層級（山原選舉區 1998／2002／2005 皆 30 個；平原 15／16／17）。
合併類別被關在 `T2/T3 × prv` 這一格裡，每屆固定 2 席（臺北、高雄各 1）。
直轄市那支的 `elbase` **沒有選舉區層級**，`elcand` 的選舉區欄全部是 `00`，
**沒有任何欄位可以拆出山地／平地**。

因此不需要「實體隨時間分裂」的建模：縣市腿直接延長，直轄市腿與 1994 省議員
各自另列。外部覆核（Codex 與 agy）在更正前提後皆同意此方向。

### 其他已確認的事實

- **編碼形式**：1994–2006 無引號、無撇號、合計列在**首列**、版面皆為「合計在前」、
  五個檔的欄數全符。技術上與 2014／2018 只差在引號與合計列位置。
- **`idx6 + idx7 = idx8`**（有效＋無效＝投票數）在所有屆別皆成立。
- ⚠️ **1994 與 1998 的合計列投票率不可靠**：1994 平原2 檔案值 63.25 但重算 53.47
  （該值與 1994 山原完全相同）、1994 直轄原 74.26 但重算 68.99（與 1998 直轄原相同）、
  1998 平原差 0.43、1998 山原差 0.03。**明細列則全部正確**——問題只在合計列。
- 2002／2005／2006 幾乎全對，各僅 1 列進位臨界差 0.01–0.02。
- **1994 台灣省議員的「平原」與「平原2」是兩個選舉區**，不是重複檔
  （候選人完全不同：平原為黃金文、楊仁福；平原2 為林昌榮、胡德夫、林正二）。

### ⚠️ 納入時處理掉的來源陷阱（皆為實測）

下列四項是 2026-08-19 清點時抓到的。實際動手納入後又抓到四項，
合計八項**全部具名記錄**於 `data/sources.json` 的
`cec-votedata.legacyTerms1994_2006.knownDefects`，處置與補償性檢查見
`openspec/archived/`（或 `openspec/changes/`）的 `include-1994-2006-terms/design.md`。

**一、代碼欄有尾隨空白。** 舊檔的行政區代碼寫成 `'0 '`、`'1 '`。現行 `read_csv`
只在 `quoted=True` 時 `.strip()`，而 1994–2006 是 `quoted=False`。不處理的話
`admin_level()` 會把**每一列都判成「投開票所」**，彙總列與明細列全部混在一起，
而且數字看起來完全正常。

**二、1998／2002 的縣市代碼在各檔別內部重新編號。**

| 檔 | 縣市數 | 與同屆「區域」檔代碼不一致 |
| --- | ---: | ---: |
| 1998 山原 | 12 | 6 |
| 1998 平原 | 10 | 9 |
| 2002 山原 | 12 | 6 |
| 2002 平原 | 11 | 10 |
| 2005 山原／平原 | 12／12 | 0 |

例：1998 平原檔的 `002` 是桃園縣，區域檔的 `002` 是宜蘭縣。**2005 起才改用全域代碼。**
跨檔別用 `(省市, 縣市)` join 在 1998／2002 會**靜默對錯縣市**，
而名稱 join 因 Big5 私用區字元不可行——需要逐檔 crosswalk。

**三、`elprof` 的人口數欄只在縣市層級以上適用。** 用「人口數 ≥ 選舉人數」逐列檢查
清點的 17 個檔：**檔別合計（0/17）與縣市層級（0/145）零違反**，
選舉區 1/607（唯一那列在 1998 區域對照檔），鄉鎮市區以下違反 9,810/21,672 列。
症狀是常數與流水號——1998 山原的 226 個鄉鎮市區列中有 **76 列**同為 `1888`；
**2002 平原與 2002 區域的鄉鎮市區列全部是 `1888`**（211/211、348/348）；
**1994 省議員區域檔是 `81169`×322**；2005 區域是 `1234`×180；
2006 直轄市是連號 `1234`–`1256`；2005 山原有「選舉人 255、人口 286322」。
另：**2002 山原有 8 個、平原有 1 個帶小數的估算值**（全國 `206740.121634792`、
屏東縣 `49115.2145361076` 等，全在檔別合計與縣市層級），`int()` 會直接拋例外。

⚠️ **這不是舊屆專屬問題。** 2009–2022 四屆在村里（76,517/84,275）與
投開票所（123,658/176,811）層級也大量違反，但那些列的人口數**全部是 0**——
是「來源未填」，不是「數字錯」。舊屆鄉鎮市區則是「填了但是常數」。

⚠️ **「適用」不等於「數值已驗證」。** 檔別合計與縣市層級也出現帶小數的值，
而戶籍人口是離散的人頭計數、不可能有小數——該數字顯然經過推算，不是原始統計值。
本專案對這一欄任何層級的數值正確性**不作保證**，輸出時另附 `人口數適用層級` 欄。
處置：原樣保留字串、不轉型、不重算。

**四、2005 縣市議員的 `elcand` 當選註記壞掉。** 逐選舉區三源比對：

| | elprof vs elctks | elprof vs elcand |
| --- | ---: | ---: |
| 2005 山原（30 區） | **29 區相符** | 18 區相符 |
| 2005 平原（17 區） | **17 區全符** | 6 區相符 |

唯一不符的屏東縣第十六選區：elcand 有 1 位候選人、elctks 無該區任何列、
elprof 說 1 席——**同額競選未投票**，三者其實一致。
1998／2002 兩檔則完全一致（30/23、30/26），**只有 2005 這一屆壞**。

`elprof` 的候選人數（66／65）與 `elcand` 列數完全相符，所以 elcand 的其餘欄位沒問題，
只有當選註記那一欄爛掉。**2005 的正確席次是山原 30、平原 27**，以 `elprof`＋`elctks` 為準。

這會同時撞上現有的第 4 項驗證（`build_local_election.py:529`，elprof 當選人數 vs
elcand 列數）與第 7 項（`:670`，兩檔註記一致）——**兩個驗證都正常運作，會直接中止**。
處置沿用 `KNOWN_ANOMALIES` 的既有模式：**elcand 的註記原樣保留**，另立由
elprof＋elctks 推導的權威欄位，並配補償性檢查（逐選舉區當選人數必須完全吻合、
差額為 0）。**不覆寫來源數字**——外部覆核曾建議 hardcoded patch 覆寫，未採納，
理由是放寬或覆寫之後新錯誤會藏進舊例外裡。

### 補完後的議員席次序列

| 屆 | T2（平原） | T3（山原） | 直轄市合併 |
| --- | ---: | ---: | ---: |
| 1994 | 2（省議員，分兩選舉區） | 2（省議員） | 2 |
| 1998 | 23 | 30 | 2 |
| 2002 | 26 | 30 | 2 |
| 2005／2006 | 27 | 30 | 2 |
| 2009-2010 | 31 | 32 | — |
| 2014 | 33 | 33 | — |
| 2018 | 34 | 35 | — |
| 2022 | 34 | 35 | — |

**議員對議員，1998→2022 是連續的。** 已上線資料裡「原住民席次總計」
2009-2010 的 63 → 2014 的 190 那個跳點，來自 D2（區長）、R3（區民代表）、
R2（鄉鎮市民代表）在 2014 才納入——**那是不同職位，不是同一職位的跨屆變化**。
圖表必須**按職位分線**，否則會被讀成席次暴增。

⚠️ **1994 的原住民席次分屬兩種職位**：台灣省議員（山原 2 席／平原 2 席，
分兩個選舉區各 1 席）與直轄市議員合併類別（2 席）。台灣省議會於 1998 年精省後廢除，
且省議員的 `elbase` 只有「臺灣省」一列。壓縮檔內**沒有 1994 縣市議員資料夾**
（該選舉是否舉行過未查證，只能說 zip 裡沒有）。因此 1994 是 6 席、1998 是 55 席，
**畫成同一條折線會被讀成參政權在 1998 爆炸性擴張**，實際只是 1994 資料集
沒有收錄縣市議會層級。1994 建進資料層但不進任何折線。

### ✅ 更正：1998 沒有異常

先前 README 記載「1998 縣市議員的山原與平原找不到合計列，是 21 個探勘目標中
唯一的異常」。**那是錯的**——四個 1998 資料夾的合計列都在，而且都在首列。
錯誤來自當初探勘腳本的一個參數傳遞錯誤，並非資料異常。已重新探勘確認。


## 資料紀律

沿用相鄰專案的原則：

1. 官方統計、學術估計、本專案計算值三者分開標示。
2. 資料不存在就標記資料缺口，不自行推估成官方數字。
3. 任何呈現的數字都要能追到來源。
4. 查不到就記「待確認」，不憑印象填。
