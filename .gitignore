# 中選會選舉資料庫：約 110MB，可從中選會重新下載，依「能不能重新取得」的判準不入庫。
data/raw/*.zip
data/raw/**/*.csv

# 組建產物與暫存
__pycache__/
*.pyc
.spectra/
openspec/.vector-search.db*

# 探索性一次性腳本，不是給人看的產出
scratch/

# spectra 依 .spectra.yaml 的 tools 設定自動生成的跨工具 skill 鷹架，
# 內容逐字相同（只有斜線指令語法照各工具慣例調整），换台機器重新生成即可，
# 不是手工維護的內容。.claude/settings.json 留待另外決定，不在此列。
.agent/
.gemini/
.claude/skills/
.claude/scheduled_tasks.lock
.claude/settings.local.json
