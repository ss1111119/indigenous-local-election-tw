# 原住民族地方選舉長表 schema

**檔案**：`data/processed/cec-local-election-summary-long.csv.gz`
`data/processed/cec-local-election-candidates-long.csv`
`data/processed/cec-local-election-votes-long.csv.gz`
**範圍**：1994、1998、2002、2005、2006、2009-2010、2014（103 年）、
2018（107 年）、2022（111 年）**九屆**，官方六種選舉種類＋本專案自訂三種
**編碼**：UTF-8 帶 BOM，換行 CRLF
**來源**：中央選舉委員會選舉資料庫 `votedata.zip`
（SHA-256 `84740535…`，登記見 [`data/sources.json`](../../data/sources.json)）
**授權**：政府資料開放授權條款第 1 版

| 代碼 | 選舉種類 | 原住民專屬 | 檔別 |
| --- | --- | :---: | --- |
| `T2` | 議員(平地原住民)選舉 | ✔ | `city` ＋ `prv` |
| `T3` | 議員(山地原住民)選舉 | ✔ | `city` ＋ `prv` |
| `D2` | 直轄市原住民區長選舉 | ✔ | `單一` |
| `R3` | 直轄市原住民區民代表選舉 | ✔ | `單一` |
| `R2` | 鄉(鎮、市)民代表(平原)選舉 | ✔ | `單一` |
| `T1` | 議員(區域)選舉 | ✘ **對照組** | `city` ＋ `prv` |

⚠️ **下列三個代碼是本專案自訂的，不是中選會的代碼。** 官方對照表只涵蓋 2022 年，
這兩類職位在其中根本不存在。代碼刻意含**連字號**——官方代碼一律兩字元英數，
連字號本身就是「這不是官方代碼」的標記。

| 代碼 | 選舉種類 | 屆別 | 為什麼不能併進 T2／T3 |
| --- | --- | --- | --- |
| `T-PRV3` | 臺灣省議員(山地原住民)選舉 | 1994 | 省議會 1998 年精省後廢除，與縣市議員是不同職位 |
| `T-PRV2` | 臺灣省議員(平地原住民)選舉 | 1994 | 同上。「平原」與「平原2」是兩個選舉區，不是重複檔 |
| `T-COMBO` | 直轄市議員(原住民，未分平地／山地)選舉 | 1994／1998／2002／2006 | **合併類別**，T2 與 T3 互斥而它是兩者的聯集；來源無任何欄位可拆分 |

⚠️ **這三種一律 `is_main_sequence = false`，不要畫進跨屆折線。**
1994 只有 6 席、1998 有 55 席，畫成同一條線會被讀成參政權在 1998 爆炸性擴張——
實際只是壓縮檔內沒有 1994 的縣市議員資料。

選舉種類名稱採用壓縮檔內官方對照表 `選舉種類代碼.xlsx` 的原始字串，未改寫。

建置：`python scripts/build_local_election.py`。不連網。
輸出先全部算完、驗完，才一次性替換——中途失敗不會留下半套結果。

```python
import pandas as pd

s = pd.read_csv("data/processed/cec-local-election-summary-long.csv.gz", dtype=str)

# T2 的全國投票率——city 與 prv 是互斥的行政區劃分，必須相加
t = s[(s["層級"] == "檔別合計") & (s["選舉種類"] == "T2")]
t = t.astype({"投票數": int, "選舉人數": int})
print(round(100 * t["投票數"].sum() / t["選舉人數"].sum(), 2))   # 52.63
```

---

## ⚠️ 讀之前必須知道的七件事

### 一、`層級` 欄決定一切，沒有它就別加總

原始檔把**彙總列與明細列混在同一個檔案裡**。直接對得票或票數加總，
數字會暴增數倍——因為每一票被算了好幾次（投開票所一次、村里一次、
鄉鎮市區一次、選舉區一次⋯⋯）。

`層級` 欄是本專案依官方格式文件的補零規則算出來的，值有六種：

| 層級 | 意義 |
| --- | --- |
| `檔別合計` | 該檔案範圍的總計（**不是全國**，見第二點） |
| `直轄市縣市` | 縣、省轄市或直轄市 |
| `選舉區` | 該縣市內的選舉區 |
| `鄉鎮市區` | |
| `村里` | |
| `投開票所` | 最細一層 |

**要加總請先篩層級，而且要同時篩選舉種類。**

### 二、`檔別合計` 不是全國——`city` 與 `prv` 要相加

T2／T3／T1 分成兩份檔案，兩者是**互斥的行政區劃分**：
`city` 是縣與省轄市，`prv` 是六都（臺北、高雄、新北、臺中、臺南、桃園）。
D2／R3／R2 只有一份，`檔別` 標記為 `單一`。

各選舉種類的全國數字（`city` ＋ `prv` 相加）：

| 種類 | 選舉人數 | 投票數 | 投票率 | 候選人 | 當選 | 其中婦女保障 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| T2 平地原住民議員 | 202,477 | 106,573 | 52.63%* | 73 | 34 | 0 |
| T3 山地原住民議員 | 216,262 | 144,164 | 66.66%* | 80 | 35 | 0 |
| D2 原住民區長 | 31,807 | 23,712 | 74.55% | 20 | 6 | 0 |
| R3 原住民區民代表 | 31,803 | 23,712 | 74.56% | 92 | 50 | 0 |
| R2 鄉鎮市民平原代表 | 91,627 | 55,614 | 60.70% | 134 | 72 | **2** |
| T1 區域議員（對照組） | 18,710,006 | 11,445,404 | 61.17%* | 1,524 | 841 | **4** |

＊有 `city`／`prv` 兩份者，全國投票率為**本專案計算值**（合計投票數／合計選舉人數），
原始檔中不存在這個數字。單一檔者則直接取自原始檔。

### 三、⚠️ T2／T3 有將近四成是「全零列」

`summary` 表 267,876 列中有 **60,914 列（22.7%）** 是全零列——
`有效票`、`無效票`、`投票數`、`選舉人數`、`人口數` 全部為 0。

**它們全部集中在 T2 與 T3：**

| 種類 | 全零列 | 該種類總列數 | 佔比 |
| --- | ---: | ---: | ---: |
| T2 議員(平地原住民) | 8,932 | 23,180 | **38.5%** |
| T3 議員(山地原住民) | 8,199 | 21,475 | **38.2%** |
| D2／R3／R2／T1 | 0 | — | 0% |

原因：平地／山地原住民選舉人**散布在整個縣市的每一個投開票所**，
而多數投開票所根本沒有這類選舉人。那些單位仍會出現在檔案裡，但數字全為 0。
D2／R3／R2 侷限在特定的原住民區或鄉，T1 涵蓋所有選民，所以都沒有這個現象。

**這會影響什麼：**

```python
# ❌ 錯——分母含了 8,173 個沒有平地原住民選舉人的投開票所
t2 = s[(s["選舉種類"] == "T2") & (s["層級"] == "投開票所")]
t2["選舉人數"].astype(int).mean()          # 12.59（n=16,086），低估一半

# ✅ 對——先排除全零列
real = t2[t2["選舉人數"].astype(int) > 0]
real["選舉人數"].astype(int).mean()        # 25.59（n=7,913）
```

要數「有多少個投開票所」「平均每所多少選舉人」「有多少個村里」這類問題，
**一律先篩掉 `選舉人數 == 0` 的列**。要算總數（總投票數、總選舉人數）則不必，
因為零加上去不影響。

建置時會驗證「選舉人數為 0 的列必須整列為 0」——若出現「沒有選舉人卻有票」即中止。

### 四、⚠️ 不同選舉種類之間**不可相加**

這是最容易犯的錯。**D2 與 R3 是同一批選民**——實測投票數皆為 23,712，
選舉人數 31,807 與 31,803 只差 4 人。他們是那六個原住民區
（高雄市茂林、桃源、那瑪夏；新北市烏來；臺中市和平；桃園市復興）的居民，
同一天投了區長票與區民代表票兩張。

把各種類的選舉人數相加會嚴重重複計算。同理，T2 與 T3 的選民雖然互斥
（平地原住民與山地原住民身分二選一），但把他們跟 T1 相加也沒有意義——
原住民選民同時也是區域議員的選舉人。

**每個選舉種類請各自分析。** T1 放進來是作為對照組，不是要加總的。

### 五、當選人不是只有 `*`

原始檔的當選註記有**四個值**（官方格式文件明定），本表保留原值於
`當選註記`，並提供已解讀的 `當選註記語意` 與布林式的 `當選`：

| 原值 | `當選註記語意` | `當選` |
| --- | --- | --- |
| `*` | 當選 | Y |
| （空白） | 未當選 | N |
| `!` | **婦女保障當選** | **Y** |
| `-` | 因婦女保障被排擠未當選 | N |

**只數 `*` 會系統性少算當選人，而且不會報錯。** 2022 年共有 **6 席**
是婦女保障當選：R2 兩席（臺東縣卑南鄉第 03 選舉區、花蓮縣光復鄉第 04 選舉區）、
T1 四席。請一律用 `當選 == "Y"`，不要自己判斷註記。

⚠️ **本專案先前誤稱資料中未出現 `-`。** 實際上 2014 年有 12 筆、2018 年有 8 筆、2009–2010 年有 16 筆，共 36 筆。全部為男性，且全部計為未當選。
35 個出現 `-` 的選舉區**全部同時出現 `!`**（配對出現）；另有 6 個選舉區只有 `!` 沒有 `-`。

### 六、行政區代碼一律是字串，不要轉成數字

`省市`／`縣市`／`選舉區`／`鄉鎮市區`／`村里`／`投開票所` 六欄都是字串：

- **補零形式本身帶語意**：`000` 代表「該層級以上的彙總」，轉成數字就沒了
- **村里代碼首碼可能是英文**：官方格式文件說明，跨多個村里的投開票所
  由中選會另行自訂代碼（如 `A001`）。本次資料 0 列，但解析器仍支援

讀取時請用 `dtype=str`。

### 七、候選人資料已做個資最小化

原始 `elcand.csv` 含**出生日期、出生地、學歷**。中選會〈網站資料開放宣言〉
明訂：個人資料能否蒐集處理利用，使用者須自行依個人資料保護法辦理——
開放授權不免除這一條。

因此本表**不輸出**這三欄。保留的是：

- `姓名`——公職候選人姓名屬選舉公開資訊，且是識別候選人的必要欄位
- `性別`、`年齡`、`現任`、`政黨`——分析參選結構所需，識別性遠低於出生日期

要這三欄請自行回原始壓縮檔取用，並自行承擔個資法上的責任。

---

## 三張表的欄位

三張表共用前段的識別與行政區欄位：
`年度`、`選舉種類`、`選舉種類名稱`、`檔別`、
`省市`、`縣市`、`選舉區`、`鄉鎮市區`、`村里`、`行政區名稱`。

### 三張表共用的可比性標記欄位

資料橫跨九屆之後，職位、類別粒度與行政區代碼系統都不再一致。
**下游不該靠讀這份文件來過濾資料**，所以這些判斷直接標在每一列上：

| 欄 | 值域 | 說明 |
| --- | --- | --- |
| `office_type` | `縣市議員`／`直轄市議員`／`臺灣省議員`／`直轄市原住民區長`／`直轄市原住民區民代表`／`鄉(鎮、市)民代表` | T1／T2／T3 的職位取決於檔別（`city` 是縣市、`prv` 是直轄市） |
| `category_granularity` | `分平地山地`／`合併未分平地山地`／`不分平地山地（原住民區）`／`非原住民選舉種類（對照組）` | ⚠️ `合併未分平地山地` 者與 T2／T3 粒度不同，不可混計 |
| `is_main_sequence` | `true`／`false` | **能不能畫進跨屆折線。** 僅官方選舉種類為 `true`；三個自訂代碼為 `false`（共 39 列） |
| `admin_code_system` | `1994`／`1998`／`2002`／`2005+`／`2009`／`2014+` | 行政區代碼系統，六套**互不相通**。跨屆比對行政區前必須先確認此欄相同 |
| `縣市_正規化` | 3 碼 | **跨檔比對縣市請用這一欄，不要用 `縣市`。** 1998／2002 的原住民分項檔縣市代碼在各檔內部重新編號，此欄已換算為同屆「區域」檔口徑 |
| `鄉鎮市區_正規化` | 3 碼或**空字串** | ⚠️ **空字串代表「該檔的鄉鎮市區代碼是檔內重編、本專案未正規化」，不是資料缺漏。** 1998／2002／2005 的原住民分項檔皆為空。刻意留空而非放原始碼——放原始碼會讓 `(縣市, 鄉鎮市區)` 的 join 成功執行但**對錯行政區** |

⚠️ **「2005 起才是全域代碼」只在縣市層級成立。** 2005 的縣市碼與同屆區域檔
12/12 相同，但鄉鎮市區碼仍是檔內重編（山原 153/226、平原 76/224 對不上）。

### `cec-local-election-summary-long.csv.gz`（267,876 列）

選舉概況，來自 `elprof.csv`。每個選舉種類 × 每個行政層級一列。

| 欄 | 說明 |
| --- | --- |
| `層級`、`投開票所` | 見第一點 |
| `有效票`、`無效票`、`投票數` | **`有效票 + 無效票 = 投票數`**，建置時逐列驗證 |
| `選舉人數` | |
| `人口數` | **原樣保留的字串，不轉型、不重算**——舊屆有帶小數的值（2002 山原全國 `206740.121634792`），`int()` 會拋例外。底層來源未查證，見 README「尚未解決」 |
| `人口數適用層級` | `縣市以上`／`低於縣市_不適用`。⚠️ **「適用」不等於「數值已驗證」**——縣市層級也出現不可能是人頭計數的小數值。本專案對這一欄任何層級的數值正確性**不作保證** |
| `候選人數`、`當選人數` | 由版面偵測結果取出，見下 |
| `投票率` | 原始檔的值（`投票數 / 選舉人數`） |
| `版面` | 偵測到的 `elprof` idx11–16 欄位順序，保留作為可稽核性的一部分 |

層級分布：`檔別合計` 46、`直轄市縣市` 318、`選舉區` 997、
`鄉鎮市區` 5,429、`村里` 84,275、`投開票所` 176,811。

⚠️ **`有效票` 與 `無效票` 曾被本專案誤記為「投票數 男／女」。**
那個錯誤不會被「男＋女＝合計」的自我驗證攔下（`63,768 + 3,067 = 66,835` 一樣成立）。
語意以官方格式文件為準，經過為何見 README。

**版面偵測**：官方格式文件（民國 101 年）記 idx11–16 為
「候選合計, 當選合計, 候選男, 候選女, 當選男, 當選女」，但 2022 年實際資料是
「候選男, 候選女, 候選合計, 當選男, 當選女, 當選合計」。兩種各自算術自洽，
只能靠「男＋女＝合計」分辨。**兩種皆不通過即中止；兩種同時通過也中止**——
套錯會得到看起來合理但完全錯誤的席次且不報錯。

### `cec-local-election-candidates-long.csv`（7,818 列）

候選人，來自 `elcand.csv` ＋ `elpaty.csv`。

| 欄 | 說明 |
| --- | --- |
| `號次`、`姓名` | `姓名` 部分含族語名（如 `孫湯玉惠 aly saku`），與漢名同欄 |
| `政黨代號`、`政黨名稱` | 已對照 `elpaty.csv`；`999` 為「無黨籍及未經政黨推薦」 |
| `性別` | 已由 `1`／`2` 解讀為 `男`／`女` |
| `年齡`、`現任` | **`年齡` 原樣保留，含無資料值**。原始檔說明年齡可能為 `0` 或 `99`（無資料）——實測 1994／1998／2002／2005／2006 五屆共 483 位候選人整批是 `99`，2009-2010 以後四屆從未出現 `99`（範圍 23–89），全資料的 `0` 出現 0 次 |
| `年齡_有效` | **要聚合年齡請用這一欄。** 有記載放值、未記載留空。對 `年齡` 直接跑 `AVG()` 會把 483 個 `99` 當成年齡：實測平均由 **50.80**（n=7,335）被拉高到 **53.78**（n=7,818）。⚠️ `0` 與 `99` 的處置不對稱——`0` 不可能是真實年齡，任何屆別都留空；`99` 落在合法值域內，只在具名的五屆留空，否則將來真有 99 歲候選人時會吃掉真值。⚠️ 「有效」指**有記載因而可用於計算**，不是「這個年齡正確」 |
| `當選註記`、`當選註記語意`、`當選` | 見第五點。⚠️ **這三欄反映「來源怎麼寫」，含來源的已知錯誤**——見下方警告 |
| `elected_authoritative` | `true`／`false`。**跨檔比對後的當選判定。要席次請用這一欄** |
| `elected_authoritative_basis` | 權威值取自 `elctks` 的哪一個層級（`elctks_選舉區`／`elctks_鄉鎮市區`／`elctks_檔別合計`）。保留依據而非只留布林值——726 筆取自鄉鎮市區層級，依據較弱 |

⚠️⚠️ **`當選` 與 `elected_authoritative` 的分工，用錯會算錯席次。**

`當選` 由來源的 `elcand` 當選註記推導，**會忠實反映來源的錯誤**。
實測有兩處損壞：

| 檔 | `當選` 算出的席次 | 正確席次（`elected_authoritative`） |
| --- | ---: | ---: |
| 2005 縣市議員／山原 | **18** | **30** |
| 2005 縣市議員／平原 | **20** | **27** |
| 1994 直轄市原住民（高雄市） | 標錯人（吳福祥 252 票） | 高玉生（481 票，該市最高票） |

不符的 63 位候選人在建置腳本中**逐一具名**，不是整檔放行；
不符集合必須恰好等於具名集合，多一筆少一筆都中止建置。

依專案紀律**不覆寫來源數字**，所以 `當選` 與 `當選註記` 原樣保留。
健康屆別兩欄完全一致（既有四屆 7,335 筆零不一致）。

各選舉種類候選人數（九屆合計）：T2 504、T3 567、D2 59、R3 284、R2 383、
T1 5,970、T-COMBO 41、T-PRV3 5、T-PRV2 5。

### `cec-local-election-votes-long.csv.gz`（1,751,190 列）

候選人得票，來自 `elctks.csv`。每位候選人在其選舉區內的各行政層級各一列。

| 欄 | 說明 |
| --- | --- |
| `層級`、`投開票所` | 見第一點——**這張表最容易被誤加總** |
| `號次` | 對應 candidates 表的 `號次`（需同時比對選舉種類、行政區與檔別） |
| `得票數`、`得票率` | |
| `當選註記` | |

⚠️ **這張表幾乎沒有 `檔別合計` 與 `直轄市縣市` 層級**，最粗的層級通常是 `選舉區`。
因為候選人只在一個選舉區參選，跨選舉區的得票加總沒有意義。
唯一的例外是 1994 台灣省議員的三個檔（共 10 列在 `檔別合計` 層級）——
那一屆的 `elbase` 只有一列「臺灣省」，來源本身就沒有更細的層級。

要各縣市或全國的**票數**請用 summary 表的 `有效票`／`投票數`；
votes 表只回答「某位候選人在某個地方拿到多少票」。

層級分布：`檔別合計` 10、`選舉區` 7,081、`鄉鎮市區` 26,591、
`村里` 533,784、`投開票所` 1,183,724。

---

## 建置時執行的自我驗證

任何一項不通過即中止。輸出先全部算完、驗完，才**一次性替換**。

**結構層**

1. 每個檔的欄數**逐列**必須恰好符合官方格式文件（`elbase` 6／`elcand` 16／
   `elpaty` 2／`elprof` 20／`elctks` 10）。只驗「至少幾欄」會讓多出的欄位靜默通過
2. 用 `csv.reader` 而非 `split(",")`。官方文件要求資料內容不得含逗號，
   但那是對提供端的要求，不是對讀取端的保證
3. `elprof` 的行政單位鍵（6 碼）不得重複——重複列會讓後續所有加總驗證失去意義
4. 候選人複合鍵（行政區 ＋ 號次）不得重複

**數值層**

5. `有效票 + 無效票 = 投票數`（逐列）
6. 票數欄不得為負（`summary` 與 `votes` 皆檢查）；`投票數` 不得大於 `選舉人數`；
   **`選舉人數` 為 0 的列必須整列為 0**——否則「投票數 ≤ 選舉人數」會因 0 而整條跳過
7. `投票數 / 選舉人數` 重算後 ＝ 原始檔的投票率。**逐列且精確相等，不設容差**：
   中選會用四捨五入（`ROUND_HALF_UP`），不是 Python `round()` 的銀行家捨入。
   實測 54,603 個可重算的列用四捨五入全部精確吻合；用銀行家捨入則有 47 列差 0.01
   （如 17/32 = 53.125 → 53.13 vs 53.12）。慣例已確認，設容差只會讓真錯誤藏在容差裡

**跨檔對帳**

8. `elprof` idx11–16 通過「男＋女＝合計」。兩種版面皆不通過即中止；
   **兩種同時通過也中止**（例如全為 0 時），不可靜默採用第一種
9. `elcand` 的實際列數 ＝ `elprof` 的候選人數
10. `elcand` 中 `當選 == "Y"` 的列數 ＝ `elprof` 的當選人數
    （**這一項會抓到「只數 `*` 漏掉 `!`」**——實測 R2 會報 72 vs 70）
11. **逐一行政單位**：該單位所有候選人的得票加總 ＝ `elprof` 同單位的有效票。
    2022 年六種選舉共比對 71,631 個單位。⚠️ 早期版本只比對**總和**，
    那會放過互相抵銷的錯誤（A 少 10 票、B 多 10 票，總和照樣通過）
12. 參照完整性：`elctks` 出現的每個行政單位都必須存在於 `elprof`
13. `elcand` 與 `elctks` 的當選註記必須一致
14. 當選註記只能是官方定義的四個值，出現未知值即中止
15. `city` 與 `prv` 的縣市代碼不得重疊——**按選舉種類分組、組內全配對比較**
16. `votes` 的（行政單位 ＋ 號次）必須唯一——重複列會被靜默加總兩次
17. **候選人與得票的雙向參照完整性**：每位候選人至少要有一筆得票列，
    且每個得票鍵都要對應到候選人。候選人的行政區粒度隨選舉種類而異
    （T2／T3／T1 記在選舉區層級，D2／R3／R2 記在鄉鎮市區層級），
    **粒度由資料推導，不寫死種類**
18. **行政區名稱查無次數會寫進報告**，不再靜默回傳空字串——
    實測 2014 年 D2 的 `elbase` 全部 48 列選舉區欄都是 `00`，
    但 `elprof` 有 90 列是 `01`，**來源自身不一致**，查名稱必然落空。
    這 417 次查無先前完全沒有跡象

---

## 測試

`python scripts/test_build_local_election.py`（也可用 pytest）跑 127 項：
單元測試涵蓋層級判定、版面偵測、CSV 讀取、當選註記、gzip 可重現性；
迴歸測試把六種選舉的實際數字釘死（列數、層級分布、逐種類全國數字、婦女保障席次）。

**已用變異測試確認測試套件真的會失敗。** 三個變異各自被不同的機制擋下：

| 變異 | 建置 | 測試 |
| --- | --- | --- |
| 把 `!` 從當選判定拿掉 | **失敗**（R2 報 72 vs 70） | 失敗 |
| 拿掉投開票所層級判定 | 通過 | **失敗**（層級分布不符） |
| `city`/`prv` 檢查不按種類分組 | **失敗** | 通過 |

⚠️ 第三個變異一開始**沒被抓到**，因為原本的檢查寫死 `sets[0] & sets[1]`
只比對前兩份，而那剛好就是同一種類的 `city`／`prv`。改為組內全配對後才擋下。
**變異測試抓到的是驗證本身的漏洞，不是資料的問題。**
